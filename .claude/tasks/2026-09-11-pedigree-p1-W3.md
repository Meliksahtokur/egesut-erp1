# W3 görevi — Task 2: farm nodes backfill + maternal edges + integrity report (gate G2)

Goal: `.harness/goals/2026/G-20260911-PEDIGREE-P1-TEMEL.md` (aktif goal; DB
kuralları ve durdurma koşulları orada).

Önkoşul (lead sağlamıştır): tabanında Task 0.5 (`dogum.buzagi_id`, migration
20260911000001) VE Task 1 (pedigree foundation, migration 20260911000002)
merge'li — kendi tabanında `git log --oneline | grep -c 'buzagi_id\|pedigree'`
görebilirsin; göremiyorsan dur ve ss-ask ile lead'e sor.

## Otorite dokümanlar — OKU

1. `.claude/plans/2026-09-10-pedigree-genetics-impl.md` **yalnız L689-907**
   (Task 2: 2.1 node backfill, 2.2 maternal edge, 2.3 integrity report, 2.4
   idempotency) — implementasyon otoritesi
2. `.claude/plans/2026-09-10-pedigree-genetics-impl.md` **L506-556 yalnız
   "Task 2 (integrity)" grant bloğu** — `GRANT EXECUTE ON FUNCTION
   public.pedigree_integrity_report() TO authenticated` SENİN migration'ında
   durur (r2-N1 kuralı)

Kapsam dışı: `js/` YASAK; Task 0.5/1 dosyalarını DEĞİŞTİRME (read serbest);
Task 3+ bölümlerini uygulama.

## Üreteceğin dosyalar

```text
supabase/migrations/20260911000003_pedigree_farm_backfill.sql
tests/sql/pedigree_backfill_test.sql
```

Numara 20260911000003 **sabit** — lead atandı (W1 ...0001, W2 ...0002 sonrası).

## Kontrat özeti (ayrıntı plan L689-907 — oradan sapma)

- **2.1 Node backfill:** tüm `hayvanlar` → `pedigree_ensure_farm_node(id)`,
  idempotent; render alanları kopyalanmaz (label çözümü projection join'inde).
- **2.2 Maternal edge:** YALNIZ planın tam SQL koşulu (buzagi_id FK predikatı):
  `c.anne_id IS NOT NULL AND EXISTS(dam) AND c.dogum_tarihi IS NOT NULL AND
  EXISTS(dogum d WHERE d.buzagi_id = c.id AND d.anne_id = c.anne_id AND
  d.tarih <= c.dogum_tarihi)`; `source_type='reconcile'`, `confidence=1`.
  Kupe-join YOK. İki dışlama sınıfı: `maternal_tarihsel_uyumsuz` (blocker) /
  `maternal_tarih_bilinmiyor` (warning). "En geç kayıt" yorumu YOK.
- **2.3 `pedigree_integrity_report()`:** read-only, tek JSON; **18 kodun ta
  kendisi** grup evreni (plan L770-810 listesi birebir); emisyon kuralları
  (bulgusuz grup HİÇ emit edilmez; `{count:0}` item'i YOK; cutoff_invalid tek
  item); `post_cutoff_null_semen` ayrım kuralı (yalnız sperma dolu + semen_id
  NULL); cutoff okuma `pedigree_try_timestamptz` helper'ıyla exception-güvenli
  (anahtar yok→`tanimsiz` [grup hiç emit edilmez], bozuk→`gecersiz` [blocker]);
  severity matrisi plan L836-848'den birebir; yeni üç kontrolün predikat
  kontratı L850-884; item şeması yalnız `{"key","detail"}`.
  `dogum_anne_graph_dam_celiskisi` buzağı node'unu doğrudan `buzagi_id` FK'sından
  çözer; `buzagi_id` NULL → o grupta bulgu yok (`dogum_buzagi_missing` info'da
  görünür).
- **2.4 Idempotency:** backfill iki kez koşulunca node/edge sayısı artmaz.
- **Replay-safe** idempotent DO blokları; grant kendi migration'ında;
  `SECURITY DEFINER + search_path = public, pg_temp` (rapor RPC de).

## Test dosyası (`tests/sql/pedigree_backfill_test.sql`)

`BEGIN ... ROLLBACK`; koşum `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f ...`.
Kapsam (goal G2): fixture veriyle rapor blockers==0; 18-kod emisyon kuralları
(bulunmayan grup emit edilmiyor; cutoff `tanimsiz` post-cutoff grubu atlıyor;
bozuk cutoff `'1900-13-45'` → istisna değil `gecersiz`); maternal edge'in
buzagi_id FK predikatına bağlılığı; idempotency (ikinci koşum Δ0).

## Ortam (worktree'de .env/node_modules yok — bootstrap)

```bash
ln -sfn /home/melik/egesut-erp1/node_modules node_modules
ln -sf  /home/melik/egesut-erp1/.env .env
set -a; source .env; set +a
DATABASE_URL="postgresql://postgres.${SUPABASE_DEMO_REF}:${SUPABASE_DEMO_DB_PASSWORD}@${SUPABASE_DEMO_POOLER}:5432/postgres"
```

Demo ref `vtzqjmazsvurxdeondmi` — PROD DEĞİL, erişimin YOK. Demo DB PAYLAŞIMLI:
sayaç dalgalanması normal, deterministik test davranışına bak. Migration'ı
demo DB'ye uygula (serbest); geçici dosyada `mktemp -d` (TMPDIR).

## Kabul (G2) — hepsi kanıtlı

1. `pedigree_backfill_test.sql` demo DB'de yeşil (exit 0; çıktı teslim notunda).
2. Migration demo DB'de uygulandı + replay-safe (ikinci koşum no-op).
3. Demo sürüde backfill sonrası rapor: `blocker` item sayısı ve varsa blocker
   kodları teslim notunda (gerçek sürü verisi — fixture değil).
4. `npm run test:unit` — taban 737/736/1 (known-red gecmis-pipeline:283);
   YENİ kırmızı yok.
5. Self-review: `git diff` okundu; yalnız 2 hedef dosya.
6. Kendi dalına commit; kırıntı yaz (`.ss/`, `ss-crumbs`, workspace adınla).

Teslim = kendi dalında commit + teslim notu (kanıt çıktılarıyla). Sorun
çıkarırsa durma, `ss-ask --class scope` ile lead'e soru dondur.
