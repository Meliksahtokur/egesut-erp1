# W1 — Task 3: Pedigree projection RPC (SQL)

Goal: `.harness/goals/2026/G-20260911-PEDIGREE-P2-AGAC.md` (kabul G3 + migration disiplini)
Plan otorite: `.claude/plans/2026-09-10-pedigree-genetics-impl.md` Rev 3.1, **Task 3**.
Çelişkide goal kazanır. Lead kapı notu: plandaki iki nokta zarf düzeyinde düzeltildi (B1 aşağıda).

## Tek iş — projection RPC'leri + SQL fixture

1. **Migration `supabase/migrations/20260911000004_pedigree_projection_rpc.sql`**
   - B1 DÜZELTMESİ (lead kapı bulgusu): planın öngördüğü `20260910000003` adı ÇAKIŞIYOR —
     P1 fiilen `20260911000002` (foundation) + `20260911000003` (farm_backfill) attı ve
     planın adı sözcüksel sıralamada foundation'dan önce düşer. **Tam olarak
     `20260911000004` kullan** (veya üzeri bir 20260911* adı).
   - `pedigree_subgraph(p_focus_node_id uuid, p_ancestor_depth integer default 4, p_descendant_depth integer default 1) returns jsonb`
   - `pedigree_subgraph_for_animal(p_hayvan_id text, p_ancestor_depth integer default 4, p_descendant_depth integer default 1) returns jsonb`
   - Dönüş kontratı (W3 buna karşı kodluyor — BİREBİR koru):
     `{focus: uuid, nodes: [{id, kind: farm_animal|external_animal, farm_animal_id: <hayvanlar.id|null>, label, sex, breed, birth_date}], edges: [{id, source(parent), target(child), role: dam|sire, source_type: birth|…}], meta: {ancestor_depth, descendant_depth, truncated}}`
     — farm alanları `hayvanlar`'dan resolve, external kendi alanından; `meta` clamp'lenmiş
     EFEKTİF değerleri taşır (goal: effective_depth yanıtta).
   - Guard'lar: negatif depth reddi; max ancestor 8 / max descendant 3; same-farm check;
     recursive CTE `path uuid[]` cycle guard (döngüde termination).
   - Migration disiplini: replay-safe; farm_id DEFAULT + öncül index; grants
     **authenticated-only, AYNI migration'da** (service_role'a EXECUTE YOK — P1 F5 dersi).
2. **SQL fixture `tests/sql/pedigree_projection_rpc_test.sql`** — `tests/sql/` mevcut desenine
   uygun; kapılar: focus-only, 4-kuşak zincir, shared ancestor dedup, sibling descendants,
   unknown parent, max depth truncate, döngüde termination. Depth clamp 0-8.
3. **Demo DB'de koş ve yeşile getir** — P1'in fixture verisi (166 node/59 edge) üzerinden
   focus'lu alt graf. `.env` (symlink) demo erişimi verir. **PROD YASAK.**

## Kabul komutları

- Migration demo'ya uygula + fixture'ı koş → tüm case'ler yeşil; komut + çıktı özeti teslim notunda.
- Depth clamp davranışı ölçülü olmalı (max 8 üstü istek → clamp + meta yansımaları).

## Sınırlar

- `js/`, `index.html`, `vendor/`, `tests/unit/`, `tests/support/` DOKUNMA (W2/W3 yüzeyi).
- Task 8+ (semen identity) P3 — yok. ELK yok. IDB yok.

## Şerit kuralı (owner 2026-09-11, ZORUNLU)

Teslimden ÖNCE kendi diff'ine builtin subagent review koştur; teslim notunda
`bulgu: <liste>` ya da `bulgu yok` yaz. **Notsuz teslim reddedilir.**

## Teslim

- Kendi dalına (`idle/pedigree-p2-W1`) commit'le. Push/merge YOK — lead kapısı.
- Kırıntı: `/home/melik/egesut-erp1/.crumbs/pedigree-p2-w1.jsonl` (rol sözleşmen §Kırıntı).
- Soru: `ss-ask --class scope`.
