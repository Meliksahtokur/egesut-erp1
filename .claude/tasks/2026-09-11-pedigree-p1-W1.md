# W1 görevi — Task 0.5: `dogum.buzagi_id` foundation (gate G0b)

Goal: `.harness/goals/2026/G-20260911-PEDIGREE-P1-TEMEL.md` (aktif goal; DB
kuralları ve durdurma koşulları orada).

## Otorite dokümanlar — OKU (sırayla)

1. `.claude/specs/2026-09-11-dogum-buzagi-id-teklif.md` — **birebir kontrat**
   (Rev 2: SET NULL + partial unique + konservatif backfill §2b)
2. `.claude/plans/2026-09-10-pedigree-genetics-impl.md` **yalnız L339-377**
   (Task 0.5 bölümü) — teklifle birlikte uygulanır

Kapsam dışına çıkma: Task 1/2 bölümlerini okuma, pedigree dosyalarına dokunma,
`js/` dosyalarına dokunma.

## Üreteceğin dosyalar (yalnız bunlar)

```text
supabase/migrations/20260911000001_dogum_buzagi_id_foundation.sql
tests/sql/dogum_buzagi_id_test.sql
```

Numara 20260911000001 **sabit** — lead tarafından atandı (W2 ile çakışma
önlemi; next-free arama).

## Kontrat (teklif §2/§2b'nin 5 maddesi)

1. `ALTER TABLE public.dogum ADD COLUMN buzagi_id text NULL REFERENCES
   public.hayvanlar(id) ON DELETE SET NULL` (hayvanlar.id text — canlı S4).
2. `CREATE UNIQUE INDEX dogum_buzagi_id_uidx ON public.dogum (buzagi_id)
   WHERE buzagi_id IS NOT NULL`.
3. `dogum_kaydet` aynı transaction'da yazar. Gövde kaynağı: **demo DB canlı
   gövdesi** (`pg_get_functiondef`), repo migration tarihinden değil. Mevcut
   sıra dogum-önce ise: `INSERT dogum (buzagi_id NULL) RETURNING id` →
   `INSERT hayvanlar ... RETURNING id` → `UPDATE dogum SET buzagi_id=...`.
   Canlı gövde ile repo GT arasında ayrışma görürsen düzeltme YAPMA —
   teslim notunda açık kalem olarak raporla.
4. Konservatif backfill (§2b): yalnız `kupe exact + dogum.tarih ==
   hayvanlar.dogum_tarihi + TEK aday` → AUTO yaz; çok-aday / tarih-uyumsuz /
   aday-yok → NULL bırak. Eski 4 kriterli ranking FK'ya YAZMAZ. Backfill
   raporunda auto / cok-aday / tarih-uyumsuz / aday-yok **dört sayacı** yazılır.
5. `geri_al` uyumu: SET NULL FK'i kendisi temizler; prosedürel null'lama YOK.

## Test dosyası (`tests/sql/dogum_buzagi_id_test.sql`)

Mecburi desen: `BEGIN ... ROLLBACK` bloğu (tracked örnek:
`tests/sql/hayvan_grup_padok_sync_test.sql:1-5`), fixture kalıcı satır
bırakamaz. Koşum: `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f ...`.
Kapsam (goal G0b): unique ihlali reddi (aynı buzagi_id iki satırda) / SET NULL
davranışı (calf DELETE → dogum kalır + buzagi_id NULL) / konservatif backfillin
4 sınıfı / `dogum_kaydet`'in aynı-transaction yazımı.

## Ortam (worktree'de .env/node_modules yok — bootstrap)

```bash
ln -sfn /home/melik/egesut-erp1/node_modules node_modules
ln -sf  /home/melik/egesut-erp1/.env .env
set -a; source .env; set +a
DATABASE_URL="postgresql://postgres.${SUPABASE_DEMO_REF}:${SUPABASE_DEMO_DB_PASSWORD}@${SUPABASE_DEMO_POOLER}:5432/postgres"
```

Koşum öncesi hedef doğrula: demo ref `vtzqjmazsvurxdeondmi` (PROD
`zqnexqbdfvbhlxzelzju` DEĞİL). **PROD erişimin YOK** — gerekirse `ss-ask` ile
root'a sor. Migration'ı demo DB'ye UYGULA (serbest); test fixture'ı ROLLBACK'le.
Geçici dosya gerekirse `mktemp -d` (TMPDIR'e saygı; `/tmp`'ye sabit yazma).

## Kabul (G0b) — hepsi kanıtlı

1. `dogum_buzagi_id_test.sql` demo DB'de yeşil (exit 0; çıktı teslim notunda).
2. Migration demo DB'ye uygulandı; replay-safe (ikinci koşum no-op).
3. Backfill 4 sayacı ölçüldü (sayaç değerleri teslim notunda).
4. `npm run test:unit` — 737 test, 736 pass, 1 fail (known-red
   `tests/unit/gecmis-pipeline.test.js:283`) tabanıyla aynı; YENİ kırmızı yok.
5. Self-review: `git diff` okundu; yalnız 2 hedef dosya değişti.
6. Kendi dalına commit; kırıntı yaz (`.ss/` altına `ss-crumbs`, workspace adınla).

Teslim = kendi dalında commit + teslim notu (kanıt çıktılarıyla). Sorun
çıkararsa durma, `ss-ask --class scope` ile lead'e soru dondur.
