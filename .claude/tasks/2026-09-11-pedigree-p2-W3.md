# W3 — Task 6 + 7: pedigree frontend (adapter/style/view/controller + Soy & Genetik sekmesi)

Goal: `.harness/goals/2026/G-20260911-PEDIGREE-P2-AGAC.md` (kabul G3d + demo gate)
Plan otorite: `.claude/plans/2026-09-10-pedigree-genetics-impl.md` Rev 3.1, **Task 6 + Task 7**.
Çelişkide goal kazanır. Lead kapı notu: B3 (manifest dışı dar dokunuş) aşağıda.

## Kontratlar (W1/W2 paralel koşuyor — birebir bunlara karşı kodla)

**RPC JSON dönüşü (W1, Task 3):**
`{focus: uuid, nodes: [{id, kind: farm_animal|external_animal, farm_animal_id: <hayvanlar.id|null>, label, sex, breed, birth_date}], edges: [{id, source(parent), target(child), role: dam|sire, source_type: birth|…}], meta: {ancestor_depth, descendant_depth, truncated}}`
(meta clamp'lenmiş EFEKTİF değerler; depth clamp 0-8.)

**API arayüzü (W2, Task 4):**
`pedigreeApi.subgraphForAnimal(...)`, `pedigreeApi.subgraphForNode(...)`, `pedigreeApi.integrityReport()`;
network yok + cache'te var → payload; cache'te yok → açık "çevrimdışı" hatası.

Unit testler **stub-backend + kontrat fixture** üzerinden koşar (canlı RPC değil) — bu yüzden
W1/W2 beklemeden ilerleyebilirsin.

## İş 1 — `js/pedigree/` modülleri (Task 6)

- `pedigree-adapter.js`: RPC nodes/edges → Cytoscape element array; node sınıfları
  focus/farm/external/dam/sire/shared/unknown; edge data `role`/`source_type`; **HESAP YOK**
  (domain DB'de hesaplandı, adapter yalnız çevirir).
- `pedigree-style.js`: Cytoscape style config; mevcut tema token'larına uyum; farm/external
  ayrışan-erişilebilir görünüm.
- `pedigree-view.js`: init/destroy; `breadthfirst` layout config (ELK YOK); resize/re-layout;
  zoom-to-fit; node click binding.
- `pedigree-controller.js`: current focus; lazy load; "+2 kuşak"; stale-response guard
  (mevcut `_detOpenId` deyimi); farm node click → `openDet(farm_animal_id)`; external node
  click → external detail sheet; cached/offline rozeti.
- `tests/unit/pedigree-adapter.test.js`: parent→child yönü korunur; shared node duplicate
  edilmez; HTML/user stringler element data'ya güvenli girer (escX idiomu — escAttr
  ailesinden mevcut yardımcıyı kullan); missing optional field crash etmez.

## İş 2 — hayvan kartı sekmesi (Task 7)

- `index.html`: `#tab-pedigree` pane; sekmeler `Özet | Sağlık | Üreme | Soy | Görevler | Geçmiş`;
  pane içi `[Soy Ağacı] [Genetik]` — **Akrabalık sekmesi YOK** (Rev 3 hizalaması).
- **MODAL-ROUTER-01 (pattern kataloğu `.harness/patterns/index.yaml`)**: modal açma
  `data-action` dispatcher (events.js ACTIONS + handlers.js bulk kayıt) ya da üretilmiş
  HTML'de attribute `onclick` + `this.dataset` taşıma (openMWithHayvan deseni);
  `el.onclick =` DOM-property ataması **YASAK** (B22 regresyonu); kapatma her yoldan
  `closeM`; yeni confirm icat edilmez (`openConfirm` kullanılır); external detail sheet
  "non-router silent sheet" olarak serbest yüzey.
- Lazy-load: `openDet()` RPC çağırmaz; Soy tabı ilk aktive olduğunda controller tetiklenir;
  openDet değişikliği yalnızca skeleton/clear listesine `tab-pedigree` ekler + current animal
  id hazırsa ve aktif tabsa lazy tetik.
- **`?v=` DAMGASI TUZAĞI (repo dersi, goal stop koşulu):** mevcut TEK ortak değer
  `v=20260909`. Bump edersen **TÜM yerel kaynaklarda TEK ortak değer** — kısmi bump YASAK,
  F-bulgu olarak döner. Yeni yerel kaynak eklerken aynı damga değerini kullan.
- **B3 (lead kapı bulgusu):** plan "gerekirse `js/ui.js`, `js/utils/handlers.js`" diyor;
  goal manifest'i bu ikisini listelemiyor. **Dar dokunuş serbest** (openDet skeleton listesi +
  lazy tetik için gereken minimum); dokunarsan kırıntına `type: assumption` yaz, teslim
  notunda belirt — root manifest'i genişletir. Kapsam patlatma.
- `tests/support/stub-backend.js`: pedigree RPC stub'ları + çağrı sayaçları
  (şu an 0 pedigree referansı var — sen eklersin).

## Kabul komutları

- `npm run test:unit` → yeni testler yeşil + mevcut suite regresyonsuz
  (known-red gecmis-pipeline hariç). Çıktı özeti teslim notunda.
- G3d görsel kabul (lead demo gate'inde ölçülür — senden birim düzeyinde kanıt):
  adapter testleri + stub-backend sayaçlarıyla 4-kuşak focal tree akışı kilitli.

## Sınırlar

- `js/pedigree/pedigree-api.js` ve `vendor/` DOKUNMA (W2 yüzeyi); migration/SQL yok (W1).
- `js/api.js`, `js/config.js`, auth.js, DB_VER/IDB YASAK. Task 8+ / ELK / IDB yok.
- PROD YASAK.

## Şerit kuralı (owner 2026-09-11, ZORUNLU)

Teslimden ÖNCE kendi diff'ine builtin subagent review koştur; teslim notunda
`bulgu: <liste>` ya da `bulgu yok` yaz. **Notsuz teslim reddedilir.**

## Teslim

- Kendi dalına (`idle/pedigree-p2-W3`) commit'le. Push/merge YOK — lead kapısı.
- Kırıntı: `/home/melik/egesut-erp1/.crumbs/pedigree-p2-w3.jsonl` (rol sözleşmen §Kırıntı).
- Soru: `ss-ask --class scope`.
