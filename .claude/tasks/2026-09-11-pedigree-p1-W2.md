# W2 görevi — Task 1: pedigree foundation migration (gate G1)

Goal: `.harness/goals/2026/G-20260911-PEDIGREE-P1-TEMEL.md` (aktif goal; DB
kuralları ve durdurma koşulları orada).

## Otorite dokümanlar — OKU (sırayla)

1. `.claude/plans/2026-09-10-pedigree-genetics-impl.md` **L381-685** (Task 1
   bölgesi: 1.1-1.7) — implementasyon otoritesi; DDL/grant/imza detayları orada
2. `.claude/specs/2026-09-10-pedigree-genetics-architecture.md` §4 — DDL
   referansı (plan ile çelişirse PLAN kazanır)
3. `.claude/reviews/2026-09-10-live-probe-evidence.md` — canlı tip gerçeği
   (hayvanlar.id text, dogum.id uuid vb.)
4. `tests/sql/hayvan_grup_padok_sync_test.sql` — test deseni örneği

Kapsam dışına çıkma: `js/` dosyaları YASAK; `dogum` tablosuna ve W1'in
dosyalarına (`20260911000001_*`, `tests/sql/dogum_buzagi_id_test.sql`) YASAK;
Task 2+ bölümlerini uygulama.

## Üreteceğin dosyalar

```text
supabase/migrations/20260911000002_pedigree_foundation.sql
tests/sql/pedigree_graph_test.sql
scripts/db-dry-run.sh            (Task 1.7 — TMPDIR-uyumlu, tracked)
README.md veya README.tr.md      (yalnız 1 satır çağrım talimatı, Task 1.7)
```

Numara 20260911000002 **sabit** — lead atandı (W1=...0001 senden ÖNCE uygulanır;
senin migration'ın W1'e bağımlı DEĞİL, paralel geliştirilir).

**Not (lead kararı):** `scripts/` + README goal write_manifest'te yok ama plan
Task 1.7 gereği Task 1 kapsamındadır — dahil edildi; root merge kapısında
işaretlendi. Burada durman gereken bir çelişki yok.

## Kontrat özeti (ayrıntı plan L381-685'te — oradan sapma)

- **Tablolar:** `pedigree_nodes`, `pedigree_parentage`, `semen_catalog`,
  `pedigree_meta` (1.2b). `farm_id uuid NOT NULL DEFAULT '400b9107-a85e-4126-af2c-fd7fe73fb68e'`,
  indexler farm_id ile BAŞLAR. `founder_status` CHECK; sex normalize kontratı
  (`Erkek→male, Dişi→female, boş→NULL`) `pedigree_ensure_farm_node` içinde.
- **FK'ler:** `farm_animal_id ... ON DELETE CASCADE`; composite FK'ler
  `(farm_id,id)` üzerinden; `pedigree_nodes`'ta `UNIQUE (farm_id, id)` zorunlu.
- **RLS:** her tablo `ENABLE` + `USING(true)`; grant'lar NESNEYİ YARATAN
  migration'ın içinde, **tam argüman tipleriyle** (plan 1.3 listesi birebir).
  Yalnız `authenticated`; anon'a hiçbir şey yok. `semen_catalog`'a SELECT.
- **`assert_is_operator()`** (1.2b): fail-closed; `op_owner_uid` satırını
  migration YAZMAZ (owner bootstrap — deploy talimatı olarak yorum satırı).
- **Helper'lar (1.4):** `pedigree_ensure_farm_node`, `pedigree_is_ancestor`,
  `_pedigree_parent_set_core` (INTERNAL, grantsız), `pedigree_parent_set`,
  `pedigree_external_upsert`, `semen_catalog_upsert`. Tümü `SECURITY DEFINER +
  SET search_path = public, pg_temp`; farm stamp `current_farm_id()`'den.
  Advisory lock kontratı: `pg_advisory_xact_lock(hashtext(...))`.
  `p_evidence` COALESCE `'{}'` (r5-F29). `pedigree_parent_set` guard'lı
  (assert_is_operator); doğum yolu core'u doğrudan çağırır (guardsız).
- **Trigger (1.5):** `trg_pedigree_hayvan_insert` AFTER INSERT → ensure node.
- **Replay-safe:** idempotent DO blokları (ikinci koşum no-op).
- **Tooling (1.7):** `scripts/db-dry-run.sh` TMPDIR'e saygılı (`mktemp`),
  `refresh_lsp_schema.sh` betiğe katlanır ya da tracked edilir; README'ye tek
  satır çağrım.

## Test dosyası (`tests/sql/pedigree_graph_test.sql`)

`BEGIN ... ROLLBACK`; koşum `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f ...`.
Kapsam (goal G1): 1.1'deki 10 invariant + sex normalization + operatör guard
üçlüsü (non-owner manuel reddi [set_config claim mock'u] / owner geçişi /
doğum yolunun guardsız akışı) + grants authenticated-only.

## Ortam (worktree'de .env/node_modules yok — bootstrap)

```bash
ln -sfn /home/melik/egesut-erp1/node_modules node_modules
ln -sf  /home/melik/egesut-erp1/.env .env
set -a; source .env; set +a
DATABASE_URL="postgresql://postgres.${SUPABASE_DEMO_REF}:${SUPABASE_DEMO_DB_PASSWORD}@${SUPABASE_DEMO_POOLER}:5432/postgres"
```

Demo ref `vtzqjmazsvurxdeondmi` — PROD DEĞİL. PROD erişimin YOK. Migration'ı
demo DB'ye uygula (serbest); geçici dosyada `mktemp -d` (TMPDIR).

## Kabul (G1) — hepsi kanıtlı

1. `pedigree_graph_test.sql` demo DB'de yeşil (exit 0; çıktı teslim notunda).
2. Migration demo DB'de uygulandı + replay-safe (ikinci koşum no-op kanıtı).
3. `npm run test:unit` — taban 737/736/1 (known-red gecmis-pipeline:283);
   YENİ kırmızı yok.
4. Mevcut frontend etkilenmez: `js/` ve `index.html` diff'te YOK (additive).
5. Self-review: `git diff` okundu; yalnız hedef dosyalar.
6. Kendi dalına commit; kırıntı yaz (`.ss/`, `ss-crumbs`, workspace adınla).

Teslim = kendi dalında commit + teslim notu (kanıt çıktılarıyla). Sorun
çıkarırsa durma, `ss-ask --class scope` ile lead'e soru dondur. Tesliminden
sonra lead TEK bağımsız codex (luna max) review turu açar — bulgular sana döner.
