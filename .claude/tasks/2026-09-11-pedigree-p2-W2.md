# W2 — Task 4 + Task 5 P2-bandı: pedigree-api.js memory cache + vendor cytoscape

Goal: `.harness/goals/2026/G-20260911-PEDIGREE-P2-AGAC.md` (kabul G3b + G3c)
Plan otorite: `.claude/plans/2026-09-10-pedigree-genetics-impl.md` Rev 3.1, **Task 4 + Task 5 P2-bandı**.
Çelişkide goal kazanır. Lead kapı notu: planın cache-sabiti noktası zarf düzeyinde düzeltildi (B2 aşağıda).

## İş 1 — `js/pedigree/pedigree-api.js`

- Arayüz (W3 buna karşı kodluyor — BİREBİR koru):
  `pedigreeApi.subgraphForAnimal(...)`, `pedigreeApi.subgraphForNode(...)`, `pedigreeApi.integrityReport()`
- Davranış: network RPC denenir → başarıda memory cache'e yaz; network yok/iletim hatası →
  cache'te varsa aynı payload döner, yoksa **açık "çevrimdışı" hatası**; domain hesabı
  client'a taşınmaz.
- Cache: modül-scope TEK `Map`; anahtar farm-scope
  `farm:<farm_id>:<rpc>:<focus>:<params>:v<algo_version>`.
- **B2 DÜZELTMESİ (lead kapı bulgusu):** plan Task 4.2 cache anahtarını `js/config.js`'teki
  `PEDIGREE_FARM_ID` sabitine bağlıyor — o sabit YOK ve `js/config.js` goal manifest'i
  dışında. Sabiti **modül-içi** yaz (`js/pedigree/pedigree-api.js` içinde). `js/config.js`'e dokunma.
- Invalidation: graph-write RPC'ler (`pedigree_parent_set`, `pedigree_external_upsert`,
  `semen_catalog_upsert`, `dogum_kaydet`) sonrası Map **TAMAMEN boşaltılır** (hedefli
  invalidation yok). P2'de tetikleyici yazma yolları P3'te — invalidate yüzeyi + unit test yeter.
- **YASAK:** `js/api.js`'e DB_VER/IDB dokunuşu; RPC_TABLES'a pseudo tablo; auth.js;
  epoch/protokol değişikliği (Rev 3 bunları Task 4'ten çıkardı — geri dönme).

## İş 2 — `vendor/cytoscape.min.js` (Task 5 P2-bandı)

- Sürüm **PINLE** (CDN latest yasak), vendor et; seçtiğin sürümü commit mesajına + teslim notuna yaz.
- Yalnız yerleşik `breadthfirst` layout; ELK ve diğer layout kütüphaneleri **YOK** (P4 bandı).
- Yükleme plain `<script>` (buildless) — ama index.html'e W3 karar verir; sen yalnız vendor dosyası.
- Kabul: lokal statik serverda `window.cytoscape` globali + `breadthfirst` smoke hatasız
  (komut + çıktı teslim notunda).

## İş 3 — unit testler

- `tests/unit/pedigree-api.test.js` + `tests/unit/pedigree-cache.test.js`
  (TESTING-01 deseni: `loadBrowserModule`, expose listesi — mevcut testlerden örnekle).
- Üç kilit senaryo: online çağrı → ikinci erişim cache'ten → ağ yokken YENİ görünüm
  isteği açık hata. Invalidation (write sonrası TAM boşaltma) testle kilitlenir.

## Kabul komutları

- `npm run test:unit` → yeni testler yeşil + mevcut suite regresyonsuz
  (known-red gecmis-pipeline hariç). Çıktı özeti teslim notunda.

## Sınırlar

- `index.html`, `js/ui.js`, `js/utils/`, adapter/style/view/controller DOKUNMA (W3 yüzeyi).
- Migration/SQL/DB yok (W1 yüzeyi). `vendor/` altına SADECE `cytoscape.min.js` girer.
- PROD YASAK; demo gerekirse sadece okuma.

## Şerit kuralı (owner 2026-09-11, ZORUNLU)

Teslimden ÖNCE kendi diff'ine builtin subagent review koştur; teslim notunda
`bulgu: <liste>` ya da `bulgu yok` yaz. **Notsuz teslim reddedilir.**

## Teslim

- Kendi dalına (`idle/pedigree-p2-W2`) commit'le. Push/merge YOK — lead kapısı.
- Kırıntı: `/home/melik/egesut-erp1/.crumbs/pedigree-p2-w2.jsonl` (rol sözleşmen §Kırıntı).
- Soru: `ss-ask --class scope`.
