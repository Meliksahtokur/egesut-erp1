# Denetimsiz Bug Avı Raporu — 2026-08-31

**Kapsam:** `js/api.js`, `js/app.js`, `js/ui.js`, `js/forms.js`, `js/state.js`, `js/config.js` + çağrılan Supabase RPC'leri (`.claude/rpc-reference.md` ve `supabase/migrations/99999999999999_ground_truth.sql` ile karşılaştırmalı)

**Yöntem:** 7 paralel salt-okunur subagent (dosya bazlı derin okuma + RPC imza sweep'i) → bulguların kritik kısmı orkestratör tarafından **bizzat satır okuyarak teyit edildi** (aşağıda "Doğrulama" alanı). GitNexus `query` ile execution flow'lar cross-check edildi (not: GitNexus indeks satır numaraları working tree'den bayat — `buildRpcParams` indekste 6532, gerçekte 6722; `tekrarTohumla` indekste 5939, gerçekte 6120. Indeks satırlarına değil direkt okumaya dayanıldı).

**Kısıtlara uyum:** Kod değişikliği yok, commit/push yok, Supabase'e hiçbir işlem (okuma dahil MCP) yapılmadı. Yazılan tek dosya bu rapor.

**Bilinen açık bulgu (anchor):** hafızadaki "js/auth.js:58 credential" bulgusu — satır kaymış; credential'ın tanımı `js/api.js:17`'dedir (bkz. B1).

---

## Özet

| # | Severity | Bulgu | Yer |
|---|---|---|---|
| B1 | HIGH | Demo credential + demo proje URL/anon key public bundle'da; demo DB prod verisinin güncel klonunu taşıyor | api.js:17 |
| B2 | HIGH | Android geri tuşu modal kapatma dalı ölü kod: `.modal.on` selector'ı hiçbir şeyle eşleşmiyor (32/32 modal `class="mo"`) | app.js:104 |
| B3 | HIGH | Backdrop-tap kapatma `closeM` cleanup'ını atlar → `_planliTohumlamaGorevId` sızıntısı → sonraki NORMAL tohumlama kapalı planlı göreve yazılır | handlers.js:112 |
| B4 | HIGH | UTC "bugün" (`toISOString().split('T')[0]`) İstanbul'da 00:00–02:59 arasında dünkü tarih: formlara dün tarihi basılır, bugünün doğum tarihi "ileri tarih" diye reddedilir | 12+ çağrı yeri |
| B5 | HIGH | `loadRaporlar` cases verisini `diseases` değişkenine bağlayıp yanlış alanları okur → "Aktif Vaka" kartı hep 0, kategori grafiği hep "Diğer" | ui.js:4413-4454 |
| B6 | HIGH* | Offline kuyruk replay `buildRpcParams`: 6 RPC'de parametre adları SQL imzasıyla uyuşmuyor + POST replay'de dizi obje yerine geçiyor → tüm parametreler undefined | ui.js:6688-6812 |
| B7 | HIGH* | 3 RPC her iki ground-truth kaynakta da yok (`planli_tohumlama_kaydet` vb.) — canlıda varsa temiz, yoksa özellik kırık; canlı doğrulanmalı | forms.js:283, ui.js:5661 |
| B8 | MED | `const {error} = await rpc(...)` ölü hata kontrolü kümesi (rpc() throw eder, `{error}` döndürmez) → 8 çağrı yerinde sessiz unhandled rejection | ui.js 8 yer |
| B9 | MED | `esc()`/hiç escape olmadan onclick string literal ve innerHTML'e interpolasyon — helpers.js:29-30'daki açık sözleşme ihlali; `'` içeren isimler buton öldürür, HTML enjeksiyonu mümkün | ui.js ~10 yer |
| B10 | MED | Türkçe `İ` tuzakları: düz `toLowerCase()` → `i̇` (U+0307) eşleşmez; `trLower()` var ama kullanılmıyor | ui.js/forms.js 8+ yer |
| B11 | MED | Çift global `bildirimKontrol` tanımı — forms.js:1450 (dedup'sız eskisi) kazanır → saatlik tekrarlayan bildirim spam'i | forms.js:1450 / ui.js:7769 |
| B12 | MED | `_ekUygulamalar` yalnızca başarılı submit'te sıfırlanır → terk edilen modalden taşan ilaç listesi sonraki HAYVANIN tohumlamasına stok düşümüyle yazılır | forms.js:289,309 |
| B13 | MED | VWP-confirm retry `submitInsem()` çağrısı `btn`'i düşürür → override RPC'si uçarken buton aktif; çift tık = çift tohumlama kaydı | forms.js:330-346 |
| B14 | MED | `abortKaydet`: prompt Cancel yine de abort kaydeder (bugün tarihiyle) + ileri/geçmiş tarih yalnızca format kontrolünden geçer → bozuk VWP/sessiz ankraj | forms.js:602-609 |
| B15 | MED | `submitTaskAdd` eşleşmeyen serbest metni `gorev_log.hayvan_id`'ye yazar (devlet_kupe hiç denenmez) → hayvana bağlanmayan, cron'la silinen görev | forms.js:1044-1046 |
| B16 | MED | `dbUpdate` null/`''` değerleri filtreler → alan sunucuda hiç temizlenemez; `toggleSub` geri-alında hayalet `tamamlanma_tarihi` kalıcı | api.js:190 ↔ ui.js:738 |
| B17 | MED | `syncNow` kuyrukta ilk kalıcı hatada `break` → zehirli kayıt sonrası tüm offline yazmalar sonsuza dek yerel kalır | api.js:463-475 |
| B18 | MED | `pullTables` kilidi mutex değil: bekleyenler aynı anda salınır, erken biten `finally` kilidi boşaltır → eski snapshot yeniyi ezebilir | api.js:362-367 |
| B19 | MED | Tablo bazlı pull hataları yutulur, senkron noktası yine yeşil olur → bayat veri "senkron" görünümü | api.js:406-411,447-448 |
| B20 | MED | Dashboard "Gebe ›" kartının sonucu ~250ms sonra `filterA()` tarafından deterministik ezilir | ui.js:400-405 ↔ app.js:90 |
| B21 | MED | Protokol bottom-sheet'leri pushState ile açılır ama bazı kapatma yolları `history.back()` yapmaz → öksüz history girdileri, geri tuşu desenkronu | ui.js:1033,1125,1268-1272 |
| B22 | MED | Router-modal'larında DOM-property `onclick` (bilinen bug sınıfı ihlali): `td2-tekrar-btn`, `td2-geri-al-btn`, `openConfirm` OK butonu | ui.js:6096, 751-756 |
| B23+ | LOW | 12 küçük bulgu (aşağıda) | — |

\* B6/B7'nin canlı etkisi erişilebilirliğe bağlı — ayrıntı maddelerde.

---

## HIGH

### B1 — Demo credential + demo proje anahtarları public bundle'da; demo DB prod verisinin GÜNCEL klonu

- **Yer:** `js/api.js:17-18` (tanım), `js/api.js:22-26` (demo proje routing'i), `js/auth.js:253-256` (otomatik giriş), `js/auth.js:162-167` (demo butonu her ziyaretçiye görünür)
- **Kanıt:**
  ```js
  const DEMO_LOGIN = { email: 'demo@egesut.web', password: 'demo2026' };
  window.DEMO_LOGIN = DEMO_LOGIN;
  const DEMO_URL = 'https://vtzqjmazsvurxdeondmi.supabase.co';
  const SB_URL   = IS_DEMO ? DEMO_URL : PROD_URL;
  ```
- **Neden kırılır:** Credential, demo proje URL'si ve anon key GitHub Pages bundle'ında her ziyaretçiye açık. Demo modu ayrı bir Supabase projesine yönlendirir (prod tablolarına YAZMA yolu değildir), ancak `demo_klonla` RPC'si (demo.js'deki "↻ Prod'dan Klonla" butonu, sunucu tarafı FDW) demo DB'yi **prod verisinin güncel kopyasıyla** tazeler. Sonuç: herhangi bir internet kullanıcısı demo butonuna basarak (veya bundle'dan URL+key+credential çıkarıp doğrudan REST'e vurarak) **gerçek sürü/tedavi/stok kayıtlarını okuyabilir**; paylaşılan demo durumunu bozabilir ve `demo_klonla` tetikleyebilir (yalnızca client-side confirm).
- **Tetik:** Demo butonu / `?demo` URL paramı — kimlik doğrulama gerektirmez.
- **Fix yönü:** Klon verisini maskele/sentezle; `demo_klonla`'yı yetki/rate-limit arkasına al; credential'ı "public-by-design" olarak dokümante et.
- **Doğrulama:** bizzat (api.js:17 grep + auth.js tam okundu). Hafızadaki "auth.js:58" referansı satır kaymasıydı — konum api.js:17'ye düzeltildi.

### B2 — Android geri tuşu modal kapatma dalı ölü kod

- **Yer:** `js/app.js:104` (selector), `js/utils/modal.js:4-8` (gerçek sınıf), `index.html` (32/32 modal `class="mo"`)
- **Kanıt:**
  ```js
  const openModals = document.querySelectorAll('.modal.on');   // app.js:104
  // openM: el.classList.add('on') — el'in sınıfı "mo"; iç sayfada class="modal" var ama hiç "on" almaz
  ```
- **Neden kırılır:** `.modal.on` hiçbir öğeyle eşleşmez (`.mo` ≠ `.modal`; `modal.js:5-6` ve index.html CSS `.mo.on{display:flex}` teyit). popstate modal dalı asla çalışmaz; her `closeM()` → `history.back()` → popstate → `goTo(e.state?.pg || 'dash', false)` (app.js:145) alttaki sayfayı yeniden render eder. İki somut belirti: (1) Android geri tuşu açık modal'ı kapatmak yerine altındaki sayfayı gezdirir, modal ekranda kalır; yığılmış modal kapatınca (`{modal:…}` state'inde `.pg` yok) uygulama dashboard'a atlar. (2) Sürü sayfasında her modal kapanışı `goTo('suru')` → `fchipReset(); filterA()` çalıştırır → **kullanıcının aktif grup/arama/padok filtreleri silinir**.
- **Tetik:** Herhangi bir modal açıkken Android geri tuşu; veya Sürü sayfasında filtre uyguluyken herhangi bir modal aç-kapa.
- **Fix yönü:** Selector'ı `.mo.on` yap; kapatmayı `closeM()` üzerinden yönlendir (cleanup da çalışsın).
- **Doğrulama:** bizzat (app.js:102-146 + modal.js tam + `grep -c 'class="mo"'` → 32). 3 subagent bağımsız aynı sonuca vardı.

### B3 — Backdrop-tap kapatma cleanup'ı atlar → planlı-tohumlama bayrağı sızar

- **Yer:** `js/utils/handlers.js:112` (suçlu), `js/utils/modal.js:53-60` (atlanan cleanup), `js/forms.js:282-284` (sızıntının tüketildiği yer), `js/ui.js:6494` (bayrağın set edildiği yer)
- **Kanıt:**
  ```js
  'mclose-overlay': (el, e) => { if (e.target === el) el.classList.remove('on'); },   // handlers.js:112
  // modal.js:57-60 — atlanan kod (kendi yorumu durumu anlatıyor):
  // "Aksi halde bayrak takılı kalır ve bir sonraki NORMAL tohumlama, kapanmış bir planlı göreve yazılmaya çalışılır."
  if (id === 'm-insem') globalThis._planliTohumlamaGorevId = null;
  ```
- **Neden kırılır:** Planlı tohumlama görevinden modal açılır (bayrak = gorev.id), kullanıcı karanlık arka plana dokunur → `mclose-overlay` sınıfı doğrudan söker; `closeM` çalışmaz → bayrak temizlenmez, `history.back()` de yapılmaz (öksüz history girdisi de kalır). Kullanıcı daha sonra **başka bir hayvan için normal tohumlama** kaydedince forms.js:283 `planli_tohumlama_kaydet`'e eski `p_gorev_id` ile yazar — kayıt yanlış (kapatılmış) planlı göreve bağlanır ve o görevi tamamlar. Aynı bypass `m-animal` form sıfırlamasını da atlar (bir sonraki "hayvan ekle" önceki hayvanın küpe/cinsiyet/grup verileriyle açılır).
- **Tetik:** Görevler → planlı TOHUMLAMA görevi → modal açılır → backdrop'a dokun → sonra normal tohumlama yap.
- **Fix yönü:** `mclose-overlay`'ı `closeM(el.id)` çağırsın (closeM, history.state eşleşmediğinde back'i zaten no-op yapıyor). app.js:107'deki ham `classList.remove('on')` dalı da aynı bypass'a sahip — B2 düzeltilirse o dal da closeM'e yönlendirilmeli.
- **Doğrulama:** bizzat (handlers.js:105-124 + modal.js tam okundu).

### B4 — UTC tabanlı "bugün": 00:00–02:59 arasında dünkü tarih

- **Yer (ana örnekler):** `js/app.js:587-588` (b/i/ta/k-tarih default), `js/utils/modal.js:11` (modal açılışında boş tarih doldurma) ve `modal.js:14` (`a-dt.max`), `js/forms.js:155` (doğum ileri-tarih reddi), `js/forms.js:272` (tohumlama reddi), `js/forms.js:602` (abort default), `js/forms.js:389,719,916,1596`, `js/ui.js:6497`, `js/ui.js:241,497,760,2028,2147,2248,2421`, `js/ui.js:4548,4614,5278,5336,5567`, `js/utils/helpers.js:8-9` (`dAgo`/`dFwd`)
- **Kanıt (bizzat okunan iki örnek):**
  ```js
  // modal.js:11 — her modal açılışında:
  if (!i.value) i.value = new Date().toISOString().split('T')[0];
  // forms.js:602:
  const bugun = new Date().toISOString().split('T')[0];
  ```
- **Neden kırılır:** Türkiye yıl boyu UTC+3; yerel 00:00–02:59 arasında UTC tarihi dünkü yerel tarihtir. Bu pencerede: (a) tüm tarih default'ları **düne** basılır (gece doğumları yanlış tarihe kaydeder); (b) `a-dt.max` dün olduğundan 00:30'da doğan buzağıya gerçek doğum tarihi verilemez; (c) forms.js:155/272 bugünün yerel tarihini "ileri tarih" diye reddeder (backend doğru yapar: `20260830000010:90` `NOW() AT TIME ZONE 'Europe/Istanbul'` — yalnızca frontend yanlış kapı); (d) "Bugün" görev filtresi ve geciken sayacı bir gün kayar.
- **Tetik:** Gece yarısından sonra 03:00'e kadar herhangi bir veri girişi (süt çiftliğinde rutin saatler).
- **Fix yönü:** Tek bir yerel-tarih helper'ı (`new Date().toLocaleDateString('sv-SE')` veya manuel Y-M-D) tüm `toISOString().split('T')[0]` "bugün" kullanımlarına.
- **Doğrulama:** mekanizma bizzat (modal.js:11, forms.js:602); çağrı-yeri envanteri 4 bağımsız subagent tarafından aynı pattern olarak listeledi.

### B5 — `loadRaporlar`: "Aktif Vaka" hep 0, kategori grafiği hep "Diğer"

- **Yer:** `js/ui.js:4413-4418` (bağlama), `:4436` (kategori), `:4454` (aktif vaka)
- **Kanıt (bizzat okundu):**
  ```js
  const [animals,tohs,diseases,births,stock]=await Promise.all([
    idbGetAll('hayvanlar'), idbGetAll('tohumlama'),
    idbGetAll('cases'),        // ← cases satırları "diseases" değişkenine bağlanıyor
    ...
  diseases.forEach(d=>{ const k=d.kategori||'Diğer'; ... });      // :4436 — cases'te kategori yok
  ${statKart('Aktif Vaka',diseases.filter(d=>d.durum==='Aktif').length,...)}  // :4454 — cases'te durum yok
  ```
- **Neden kırılır:** `cases` satırları `status` ('active'/'closed') taşır, `durum`/`kategori` taşımaz — aynı dosyanın kendisi her yerde doğru kullanıyor (ui.js:244, 828, 2013, 2027: `c.status==='active'`; kategori `diseases.category`, ui.js:3290). Sonuç: Raporlar sayfası aktif vaka olsa bile **0** gösterir (yeşil), hastalık kategorisi grafiği tüm vakaları tek "Diğer" kutusuna indirger.
- **Tetik:** Açık vakayken Raporlar sayfasını açmak.
- **Fix yönü:** `idbGetAll('diseases')` ayrı çek, `case.disease_id` üzerinden join (deseni ui.js:3043-3046'da mevcut); aktiflik `c.status==='active'`.
- **Doğrulama:** bizzat (kod bloğu + `c.status` grep teyidi).

### B6 — Offline kuyruk replay `buildRpcParams` imza yanlışları + POST'ta dizi hatası

- **Yer:** `js/ui.js:6688-6689` (RPC seçimi), `:6696-6701` (POST dizi bug'ı), `:6722-6812` (parametre builder)
- **Kanıt (bizzat okundu, seçme):**
  ```js
  const clean = op.method === 'POST'
    ? op.data.map(item => Object.fromEntries(...))    // ← DİZİ döner
    : Object.fromEntries(...);                        // ← obje
  const rpcParams = buildRpcParams(rpcName, clean, op);  // buildRpcParams data.x okur → POST'ta hep undefined
  ...
  case 'hayvan_guncelle':
    return { p_id: data[col] || val, p_alan: Object.keys(data)[0], p_deger: Object.values(data)[0] };  // p_alan/p_deger SQL'de YOK
  case 'tohumlama_kaydet':
    return { p_hayvan_id: data.hayvan_id, p_tarih: data.tarih, p_sperma_kodu: ..., p_teknisyen: ... };  // p_sperma YOK, zorunlu
  ```
- **Neden kırılır:** supabase-js bilinmeyen parametre anahtarlarını sessizce yutar → Postgres tarafında DEFAULT/NULL. Tablo: `hayvan_guncelle` (`p_alan`/`p_deger` yok → COALESCE no-op + `ok` döner → **kuyruktaki düzenleme başarı toast'ıyla kaybolur**), `hayvan_ekle` (`p_grup_id`/`p_irk_id` yok), `tohumlama_kaydet` (`p_sperma` zorunlu → PGRST hatası, kuyrukta takılır), `dogum_kaydet` (`p_kupe` zorunlu, yok), `create_case` (`p_animal_id`/`p_disease_id` hiç gönderilmez), `add_drug_administration` (`p_time` yok, saat sessizce düşer). POST replay'de `clean` dizi olduğundan doğru adlı case'ler dahil tüm `data.x` erişimleri undefined olur.
- **Erişilebilirlik notu (dürüst sınıflandırma):** Bu yol yalnızca offline kuyruk + Veri Trafik panelindeki tekil "Gönder" ile ulaşılır (otomatik `syncNow` REST kullanır). `write('hayvanlar')` çağırıcısı bugün yok (grep boş) → `hayvan_ekle`/`hayvan_guncelle` case'leri latent; diğerleri kuyruğa gerçekten giren tablolar için canlı. Kod kanıtı tam; canlı tetik sıklığı düşük-orta.
- **Fix yönü:** `buildRpcParams`'i ground-truth imzalarına göre yeniden yaz (veya şemadan üret); POST'ta `clean[0]` geç.
- **Doğrulama:** bizzat (6688-6812 aralığı tam okundu).

### B7 — Üç RPC her iki ground-truth kaynağında da yok (canlı doğrulama gerekli)

- **Yer:** `js/forms.js:283` (`planli_tohumlama_kaydet`), `js/forms.js:556` (`tedavi_sablon_tohumlama_gorev_ekle`), `js/ui.js:5661` (`vaka_tohumlama_ekle`)
- **Kanıt:** `grep planli_tohumlama_kaydet 99999999999999_ground_truth.sql` → 0 satır (bizzat); rpc-reference.md'te de yok. Tanımlar migration'larda mevcut: `20260730000001`, `20260722000002/3`, `20260730000002` — JS parametre anahtarları bu migration imzalarıyla birebir uyumlu.
- **Neden kırılır:** "Repoda migration dosyası olması = canlıda çalıştığı anlamına gelmez" (AGENTS.md). Temmuz migration'ları canlıya uygulanmadıysa PGRST203: planlı tohumlama akışı, vaka içinden planlı tohumlama ekleme hiç çalışmaz; `tedavi_sablon_tohumlama_gorev_ekle` yoksa vaka+şablon yazıldıktan **sonra** hata alır (yarı-kayıt durumu). Uygulandıysa sorun yok — yalnızca GT/rpc-reference bayat.
- **Doğrulama:** grep bizzat; canlı kontrol bu görevin yazma yasağı kapsamında tutuldu — **takip işi olarak `pg_get_functiondef` ile canlı teyit önerilir**.
- **Meta-bulgu (ayrı başlıkta):** GT'nin bayat olduğu bağımsız olarak kanıtlandı: GT:6535 `tohumlama_abort` 2-param, oysa `20260830000010:22` ve `20260830000034:9` 3-param canlı imza (rpc-reference doğru); GT:10292 `tohumlama_kaydet` DEFAULT'ları eksik; GT `hekim_listesi` için CREATE olmadan GRANT içeriyor (2609).

---

## MED

### B8 — `const {error} = await rpc(...)`: ölü hata kontrolü kümesi

- **Yer:** `js/ui.js:4168` (`stok_guncelle`), `:4184` (`stok_arsivle`), `:6518`, `:6856` (`vaccine_rapel_guncelle`), `:6867` (`hekim_ekle`), `:6982`, `:7043`, `:7060`
- **Kanıt:** `rpc()` kontratı (api.js:64-78, bizzat okundu) **throw eder ve yalnız `data` döndürür**:
  ```js
  if (error) throw new Error(_trErr(error.message));
  if (data && data.ok === false) throw new Error(data.mesaj || 'İşlem başarısız');
  return data;
  ```
  Dolayısıyla `const {error} = await rpc(...)` her zaman `undefined` destructure eder; `if(error){toast...}` dalı ölü kod. Bu 8 yerin hiçbirinde try/catch yok → gerçek hata (offline, `ok:false`, RLS) **unhandled rejection** olur: toast yok, buton yükleniyor durumunda kalır, kullanıcı Kaydet'e bastığında hiçbir şey olmamış gibi görünür. (`forms.js:425-428`'taki `result.oneri` dalı da aynı kökten: `ok:false` throw edildiği için öneri metni asla gösterilmez.)
- **Fix yönü:** try/catch + `toast(e.message, true)` (doğru deseni aynı dosyada `hekimDetSil` ui.js:6994 çiziyor).
- **Doğrulama:** rpc() kontratı bizzat; çağrı yerleri 3 bağımsız subagent tarafından listelendi.

### B9 — Escape sözleşmesi ihlalleri (onclick string literal + ham innerHTML)

- **Yerler:** onclick string içinde `esc()`/ham değer — `ui.js:1904-1905`, `:2327`, `:3454-3493`, `:7108`, `:7629`; ham (hiç escape yok) — `ui.js:666`, `:1602-1604`, `:2784`, `:2915`; innerHTML metin/attribute kaçırması — `ui.js:3185-3190` (optgroup), `:6596`, `:6846-6847`, `:6941`, `:7018`, `:5955-5958`, `:8243`
- **Kanıt:** Sözleşme kodun kendisinde (helpers.js:29-32, bizzat okundu): *"esc() tek/çift tırnak kaçırmaz, bu yüzden onclick="fn('${değer}')" kalıbında KULLANILAMAZ. escAttr() hem ' hem " hem &<> kaçırır."* Aynı dosyada doğru desen de mevcut (`data-kupe="${escAttr(kupe)}"` + `dataset`, ui.js:2368).
- **Neden kırılır:** `'` içeren küpe/padok/grup/ad → inline handler SyntaxError → satır/buton sessizce ölü (tık hiçbir şey yapmaz); `"` → attribute kırılır, markup bozulur; `<` içeren değer → enjeksiyon (tek-kullanıcı, kendi verisi — stored-XSS sınıfı, gerçekçiliği düşük ama markup bozulması gerçek).
- **Tetik:** Küpe no `TR'45` gibi; `Murat'ın` gibi padok adı; `Beta-laktam <5%` gibi ilaç adı.
- **Fix yönü:** Mevcut `escAttr()` + `dataset` desenini ihlal eden ~10 yere uygula.
- **Doğrulama:** sözleşme + iki örnek bizzat; envanter subagent kanıtı (dosya içinde zıt-desen kanıtlarıyla).

### B10 — Türkçe `İ` arama tuzakları

- **Yerler:** `ui.js:1869` (detay Geçmiş arama), `:2798-2810`, `:2997`, `:3266`, `:5967` (`acHdiStok`), `:6398`, `:7607-7609`, `:7837`
- **Kanıt:** `trLower()` helpers.js:34'te mevcut (İ→i, I→ı) ve `setupAutocomplete`/`acIlac` (ui.js:6288) doğru kullanıyor; yukarıdakiler düz `toLowerCase()`.
- **Neden kırılır:** JS'te `'İLAÇ'.toLowerCase()` → `i̇laç` (i + U+0307) → düz `i` araması eşleşmez. Büyük İ ile yazılan her arama ("İLAÇ", "İvermektin"…) sıfır sonuç döner.
- **Doğrulama:** helper bizzat; çağrı yerleri 3 subagent yakınsaması.

### B11 — Çift global `bildirimKontrol`: eskisi kazanır, saatlik spam

- **Yer:** `js/forms.js:1450` (kazanan — sonra yüklenir) ve `js/ui.js:7769` (gölgede kalan, dedup'lı M-26 düzeltmesi)
- **Kanıt:** `grep "function bildirimKontrol"` → iki dosyada da top-level `async function` (bizzat). Klasik script yüklenme sırası (index.html:2014 ui.js, 2015 forms.js) → forms.js tanımı binding'i ezer. `app.js:631-632` saatlik `setInterval(bildirimKontrol, 3600000)` yapar.
- **Neden kırılır:** Kazanan forms.js sürümünde dedup anahtarı ve Notification `tag` yok → her saat, her gecikmiş görev için aynı bildirim tekrar tekrar gönderilir; ui.js'teki tasarlanmış 3-saat-öncesi/sabah hatırlatmaları asla çalışamaz (erişilemez kod).
- **Fix yönü:** İki tanımdan birini silin (forms.js:1450 eskisi; ui.js sürümü M-26 yorumlu ve zengin).
- **Doğrulama:** bizzat grep + yükleme sırası index.html'den teyit.

### B12 — `_ekUygulamalar` modal terkinden taşar → yanlış hayvana ilaç/stok hareketi

- **Yer:** `js/forms.js:289` (gönderim), `:309` (tek sıfırlama noktası — başarılı submit), `js/utils/modal.js:60` (closeM yalnız `_planliTohumlamaGorevId`'yi sıfırlar)
- **Kanıt:** bizzat okundu — `_ekUygulamalar = []` yalnızca başarı yolunda; `closeM('m-insem')` ve modal açılışı (ui.js:6468-6473 yalnız sorun toggle'ını sıfırlar) diziyi temizlemez.
- **Neden kırılır:** Ek uygulama satırı ekleyip (örn. "PG – 2 ml") modalı X/overlay/geri ile kapatan kullanıcının listesi sekme boyunca yaşar; sonraki **farklı hayvanın** tohumlaması bu öğeleri `p_ek_uygulamalar` olarak gönderir → backend her öğe için stok hareketi oluşturur ve listeyi tohumlama satırına yazar. (Kısmi görünürlük: bayat liste `#ek-liste`'de bir sonraki açılışta görünür ama uyarı yok.)
- **Fix yönü:** m-insem açılışında (openMWithHayvan) ve/veya closeM('m-insem') içinde `_ekUygulamalar`, `_ekSeciliTur` ve ek-liste DOM'unu sıfırla.
- **Doğrulama:** bizzat (forms.js:275-351 + modal.js tam).

### B13 — VWP retry `submitInsem()` butonu düşürür → çift-kayıt penceresi

- **Yer:** `js/forms.js:330-335` ve `:342-347`
- **Kanıt (bizzat):**
  ```js
  if (btn) { btn.disabled = false; btn.textContent = 'Kaydet'; }   // confirm öncesi açıldı
  const ok = confirm(...);
  if (ok) { globalThis._vwpOverride = true; return submitInsem(); } // btn ARGÜMANSIZ — 280/351'deki tüm if(btn) guard'ları atlanır
  ```
- **Neden kırılır:** Override RPC'si ağ uçuşu sırasında Kaydet butonu aktif; ikinci tık paralel `tohumlama_kaydet` başlatır (o da override'lı — bayrak 292'de yalnız await sonrası temizlenir). Backend'de "hayvan zaten gebe" kontrolü commit öncesi yarıştığından aynı hayvana iki tohumlama kaydı + çift sperma stok düşümü + çift kontrol görevi mümkün.
- **Tetik:** Yavaş ağ + VWP onayı + kullanıcı beklemez.
- **Fix yönü:** `return submitInsem(btn);` — tek kelimelik düzeltme.
- **Doğrulama:** bizzat (275-351 okundu; not: ABORT-before-VWP sırası 326/338'de DOĞRU — orada bug yok).

### B14 — `abortKaydet`: Cancel yine kaydeder + ileri tarih kabul edilir

- **Yer:** `js/forms.js:602-609` (bizzat okundu)
- **Kanıt:** `const tarihGirdi = (prompt(...) || '').trim();` → Cancel (`null`) ile boş-OK ayırt edilmez → abort **bugün tarihiyle yine kaydedilir**. Doğrulama yalnızca format regex'i; `abortTarihi > bugun` veya tohumlama tarihinden öncelik kontrolü yok (RPC'de de yok: `COALESCE(p_abort_tarihi, CURRENT_DATE)`).
- **Neden kırılır:** Yıl tuşlaması (`2027-…`) VWP çapası olur → `tohumlama_kaydet` "-354/55 gün" gibi anlamsız uyarı üretir; aynı tarih sessiz-hayvan `MAX(...)` ankrajına girer, hayvan izlemden kaybolur.
- **Fix yönü:** `prompt()===null` → akışı iptal; ileri tarih ve `tarih < tohumlama.tarih` reddi ekle.
- **Doğrulama:** bizzat.

### B15 — `submitTaskAdd` serbest metni `hayvan_id` yazar

- **Yer:** `js/forms.js:1044-1046` (bizzat okundu)
- **Kanıt:** `hayvan = animals.find(a => a.kupe_no === hid || a.id === hid)` — dosyadaki diğer beş formun aksine `devlet_kupe` denenmez; eşleşme yoksa `hayvan_id: hayvan?.id || hid || null` → **yazılan string** (örn. devlet küpe veya yazım hatası) text kolona sessizce insert edilir, "✅ Görev oluşturuldu" toast'ı gelir. Görev hayvana bağlanamaz; `gorev_orphan_temizle` (gece cron'u) çözülemeyen hayvan_id'li görevi iptal eder → görev ertesi sabah sessizce kaybolur.
- **Fix yönü:** Lookup'a `a.devlet_kupe === hid` ekle; eşleşme yoksa engelle veya bilinçli `null` (genel görev) yaz.
- **Doğrulama:** bizzat (frontend mekanizması kesin; cron tarafı rpc-reference:233-4 yorumuna dayalı çıkarım).

### B16 — `dbUpdate` null'ları filtreler → alan temizlenemez; `toggleSub` hayalet tarihi

- **Yer:** `js/api.js:190` (filtre), `js/ui.js:738` (tetik; her ikisi bizzat okundu)
- **Kanıt:**
  ```js
  const clean = ...filter(([k, v]) => k !== 'id' && v !== null && v !== undefined && v !== '');
  // ui.js:738: write('gorev_log',{...sub,tamamlandi:nowDone,tamamlanma_tarihi: nowDone? ... : null}, 'PATCH', ...)
  ```
- **Neden kırılır:** `_writePatch` IDB'ye `tamamlanma_tarihi:null` ile yazar (yerelde temizlenir) ama sunucuya giden PATCH'ten null düşer → sunucuda eski tarih + `tamamlandi=false` kalır. Sonraki pull'da sunucu değeri IDB'yi ezer → geri alınmış görevde tamamlanma tarihi "geri döner". Aynı filtre offline kuyruk replay'inde de (api.js:467 `dbUpdate`) çalışır; genel sonuç: hiçbir alan sunucuda `''`/null'a çekilemez.
- **Fix yönü:** Filtrede yalnız `undefined`'ı atla (M-15 yorumu api.js:6691-6695'te zaten aynı kararı vermiş — tutarlılaştır).
- **Doğrulama:** bizzat (her iki uç).

### B17 — `syncNow` zehirli kuyruk: ilk kalıcı hata tüm drain'i durdurur

- **Yer:** `js/api.js:463-475` (bizzat okundu) — `catch { console.warn; break; }`
- **Neden kırılır:** Sıralı replay'de ilk kalıcı başarısız op (sunucuda silinmiş parent → FK, kısıt, yinelenen anahtar) döngüyü keser; sonraki geçerli yazmalar sonsuza dek yerel kalır; 30sn'lik arka plan sync'i ve `online` eventi aynı zehirli öğeye tekrar takılır. Ek sessiz kayıp: hedef satırı sunucuda silinmiş bir PATCH 0 satır günceller, hata vermez, kuyruktan düşer — değişiklik iz bırakmadan yutulur.
- **Fix yönü:** Öğe başına skip-and-continue (+ retry sayacı/dead-letter), kalıcı hataları senkron çubuğunda göster.
- **Doğrulama:** bizzat.

### B18 — `pullTables` kilidi mutex değil

- **Yer:** `js/api.js:362-367` (bizzat okundu)
- **Neden kırılır:** `if (_pullingPromise) await _pullingPromise;` tek sefer kontrol; ilk pull çözülünce **bekleyenlerin hepsi** aynı anda devam eder, her biri `_pullingPromise`'ı ezer; hızlı olan `finally`'de kilidi null'lar → yavaş pull'lar uçarken yeni çağıran da kilidi bypass eder. `idbClearAndPut` clear+put olduğundan geç biten eski fetch yenisini ezebilir (bayat veri, bir sonraki eventi kadar).
- **Tetik:** Çok tablolu RPC'lerde optimistic pull + realtime olaylarının üst üste binmesi (örn. `tedavi_sablon_uygula` 8 tablo → 8 realtime + 1 pull), yavaş mobil bağlantı.
- **Fix yönü:** Promise-kuyruk zinciri (her çağran kuyruk sonunu await edip işini ekler) veya await sonrası yeniden kontrol döngüsü.
- **Doğrulama:** bizzat.

### B19 — Pull hataları yutulur, nokta yeşile döner

- **Yer:** `js/api.js:406-411` (warn-and-skip), `:447-448` (dot temizleme)
- **Neden kırılır:** supabase-js tablo hatalarında reject etmez `{error}` ile resolve eder; `pullTables` loglayıp geçer, `pullFromSupabase` throw etmez → dot yeşil, outer catch çalışmaz. Donan bir tablo (token süresi, geçici view hatası) son çekim verisiyle render edilir, kullanıcıya hiçbir sinyal yok.
- **Fix yönü:** pullTables hata sayısını döndürsün; >0 ise `warn` dot/toast (sınıf zaten var).
- **Doğrulama:** subagent kanıtı (406-411 alıntısı; 362-417 aralığını kısmen bizzat okudum).

### B20 — Dashboard "Gebe ›" kartı `filterA()` tarafından ezilir

- **Yer:** `js/ui.js:400-405` ↔ `js/app.js:90` (`goTo('suru')` → `fchipReset(); filterA()`), `filterA` 250ms debounce
- **Neden kırılır:** `showGebe` önce `goTo('suru')` (senkron filterA zamanlar), sonra gebe listeyi render eder (~10-30ms); 250ms sonra debounce'lu `filterA` chip'leri sıfırlanmış tam sürüyü render eder. Deterministik: gebe listesi ~220ms görünür, sonra tüm sürü gelir — kart "çalışmıyor" görünür.
- **Fix yönü:** `showGebe` chip state'i ayarlayıp `filterA`'ya filtre olarak geçsin (renderAnimals'i bypass etmesin).
- **Doğrulama:** subagent kanıtı (her iki uç alıntılı; app.js:90 bizzat görüldü).

### B21 — Protokol bottom-sheet history sızıntıları

- **Yer:** `js/ui.js:1065` (açılış pushState) ↔ `:1033` (backdrop kapatma back yok), `:1130`/`:1125` (detay ✕ back yok), `:1268-1272` (uygulama sonrası remove + yeniden push)
- **Neden kırılır:** Sheet kaldırılsa da `{protokol:true}`/`{proto_detay:true}` girdisi stack'te kalır; bu girdiye düşen back `goTo(undefined||'dash')` → uygulama sheet listesi ekrandayken dashboard'a atlar; her uygulama bir öksüz girdi biriktirir.
- **Doğrulama:** subagent kanıtı (popstate tüketicisi app.js:121-129 bizzat okundu — `protoDetay` yalnız eleman hâlâ DOM'daysa ele alıyor).
- **Fix yönü:** Tüm kapatma yollarını open'ı aynalayan tek `closeProtoSheet()`'e yönlendir (element remove + state eşleşiyorsa `history.back()`).

### B22 — Router-modal'larında DOM-property onclick (bilinen sınıf ihlali)

- **Yerler:** `js/ui.js:6096` (`td2-tekrar-btn` → `tekrarTohumla`: `closeM` + hemen `openInsemSafe` — klasik yarış) ve aynı bloktaki `td2-geri-al-btn`; `js/ui.js:751-756` (`openConfirm` OK butonu — 11 çağrı yeri tek fonksiyondan)
- **Kanıt (bizzat):** `td2TekrarBtn.onclick=()=>tekrarTohumla(...)`; `tekrarTohumla{ closeM('m-toh-det'); openInsemSafe(kupe); }`; m-toh-det `openM` ile açılıyor (ui.js:6118) → router'a kayıtlı. AGENTS.md kuralı ve 684534f düzeltmesinin (td-hayvan) birebir anti-pattern'i; kardeş öğe `td2-hayvan` attribute+dataset kullanıyor, bu ikisi kaçmış.
- **Neden kırılır:** `closeM` → async `history.back()` sıraya girer, `openM` → `pushState` hemen çalışır; back sonradan çözülürse pointer eski girdiye döner → sonraki kapanışta çift geri gerekir / sayfa altta istenmeyen yere gider. B2'deki ölü popstate dalı belirtileri maskeliyor/büyütüyor.
- **Fix yönü:** index.html'e attribute `onclick` + `dataset.*` (td2-hayvan deseni); `openConfirm` için onConfirm'i module değişkenine alıp attribute handler çağırsın.
- **Doğrulama:** bizzat (6086-6123 + 733-757 okundu).

---

## LOW

| # | Yer | Bulgu |
|---|---|---|
| B23 | api.js:70-74 | `rpc()` catch-all her SDK istisnasını "İnternet bağlantısı gerekli" diye yeniden etiketler — ağ dışı hatalar (TypeError, auth yenileme) yanlış teşhis |
| B24 | ui.js:141-143 | `_dashVacAlerts`: `const more=total-5` — gösterilen <5 iken sayaç yanlış (kozmetik) |
| B25 | ui.js:6688 | `RPC_MAP` DELETE dalı erişilemez: seçim yalnız `.POST`/`.PATCH` okur — kuyruğa DELETE eklenirse "RPC tanımlı değil" yanılgısı (latent) |
| B26 | config.js:12 ↔ app.js:178, ui.js:6826 | `VARSAYILAN_HEKIM='H1'` sabit id — DB id'leri kayarsa select sessizce boşalırsa varsayılan hekim sessizce değişir (bugün H1 DB'de mevcut görünür; yapısal kırılganlık) |
| B27 | api.js:28-33 ↔ 369-404 | TABLES↔FETCHERS drift: `protokol_instance`/`cop_kutusu` store var fetcher yok (sessiz no-op; forms.js'te 4 `pullTables([...,'protokol_instance'])` çağrısı boşa); `tohumlanabilir_hayvanlar`/`gebelik_ozet` fetcher var store yok (ilk kullanımda NotFoundError). Latent |
| B28 | api.js:207-210, 257-264 | `write(method='PATCH')` `id=eq.` dışı filtrede null dönüp `_writePost`'a düşer → parça satı INSERT'i. Mevcut 7 çağrıcı da `id=eq.` — gelecek tuzak |
| B29 | forms.js:1395-1396 | `ilac_ekle`: `p_concentration_unit`'e konsantrasyon DEĞERİ gider ("10" gibi) — birim alanı çöp veri |
| B30 | ui.js:3127 | `link_drug_to_stock` bağlantı-kaldır: `p_drug_id:null` → `WHERE id IS NULL` → her zaman "İlaç bulunamadı" hatası (forms.js:1544 doğru yapıyor) |
| B31 | forms.js:425-428 | `kizginlık_kaydet` `oneri` alanı asla gösterilmez (rpc throw kontratı) — "Notlar bölümüne ekleyin" yönlendirmesi kaybolur (bkz. B8) |
| B32 | forms.js:1704-1705, 660, 151 | Negatif sayı kabulleri: `submitBulkIlac` `!miktar` (negatif geçer), `submitCikis` satış fiyatı, `b-dogum-kg` |
| B33 | forms.js:749 | `submitSuttenKes` `res.hata` okur, RPC `hatalar` (dizi) döner → hep "Bilinmeyen hata" fallback |
| B34 | ui.js:4563 | Görev detay çipinde ham `stok_id` UUID'si ürün adı yerine gösterilir |
| B35 | app.js:636-644 | `online` handler realtime SUBSCRIBED iken 30s polling'i koşulsuz restart eder — ağ kırpmasında polling+realtime çift çalışır ( `_syncing` guard sayesinde yalnız redundant) |
| B36 | forms.js:410-411 | `submitKizginlık` ileri tarih kontrolü yok (frontend+backend) — gelecek tarih sessiz-ankraj olur (B14 ile aynı aile) |
| B37 | app.js:636+ / api.js:486 | Çift `online` dinleyicisi (api.js + app.js) → çift `syncNow`; `_syncing` guard no-op'a indirger (bilgi düzeyi) |

---

## RPC İmza Denetimi — Özet

126 çağrı yeri tarandı (state.js/config.js'te RPC yok). **Etkileşimli üretim akışları imza bakımından temiz.** Sapmalar:

1. **GT/rpc-reference'ta olmayan 3 RPC** (B7) — canlı `pg_get_functiondef` teyidi gerekli.
2. **`buildRpcParams` kümesi** (B6) — 6 RPC'de yanlış/eksik parametre adları + POST dizi bug'ı; yalnız offline kuyruk manuel gönder yolunda.
3. **`{error}` destructure kümesi** (B8) — imza değil dönüş-kontratı hatası, 8 yer.
4. **Değer hataları:** B29 (`p_concentration_unit`), B30 (unbind NULL `p_drug_id`).
5. **Belge bayatlığı (kod değil):** rpc-reference `hayvan_ekle` için `p_padok` yazar, canlı imza `p_padok_id uuid` (frontend doğru); rpc-reference `add_drug_administration` için `p_drug_id` yazar, canlı `p_drug_product_id` (ui.js:5805 doğru); GT `tohumlama_abort` 2-param (canlı 3-param), GT `tohumlama_kaydet` DEFAULT'lar eksik, GT `hekim_listesi` CREATE'siz GRANT içeriyor. **GT yeniden üretilmeli** (canlı pg_dump).

Doğrulanmış temiz (örnekler): VWP substring sırası (forms.js:326 önce ABORT — doğru), `tohumlama_kaydet`/`dogum_kaydet`/`kizginlik_kaydet`/`cikis_yap`/`buzagi_sutten_kesme_*`/`seans_tamamla` ailesi/`sessiz_hayvanlar_listele` (jsonb dizi + sentinel sıralama UI'ta doğru)/`padok_degistir_toplu`/`asi_ekle` (13 anahtar)/`bulk_vaccination`/`geri_al` ailesi — hepsi imza ve dönüş-şekli uyumlu.

---

## Doğrulanmış Temiz Alanlar (yanlış pozitif ayıklama notları)

- authGate/init sıralaması, demo autologin döngü guard'ı (sessionStorage tek atış) — reload döngüsü yok.
- `startBackgroundSync` önceki interval'i temizler; `initRealtime` re-entry guard'lı — timer stack yok.
- Sessiz-hayvan sentinel mantığı (NULL→9999, client-side sentinel-son sıralama) RPC/UI uyumlu; `"null gündür sessiz"` dalı bilinçli olarak erişilemez (RPC zaten 9999 map'liyor).
- `openDet` bayat-render guard'ları (`_detOpenId`), `_keepScroll` finally, `yasHesapla` ödünç aritmetiği doğru.
- Çift-submit guard'ları submitAnimal/Birth/Insem(ilk geçiş)/Kizginlik/Case/Stk/StokAdd/hstIlacEkle'de mevcut (B13 istisnası hariç).
- Tedavi/reçete/seans RPC akışlarında try/catch+toast disiplini tutarlı (B8'in 8 yeri istisna).
- ID tip hijyeni: gördülen tüm canlı çağrılarda text/uuid karışımı yok.

## Kapsam Dışı Gözlemler (bilgi)

- `index.html:2096-2102`: hata paneli `e.msg`'yi escape'siz innerHTML'e basar — self-XSS düzeyi (kullanıcının kendi konsolu); ayrı turda değerlendirilmeli.
- GitNexus indeksi working tree'den bayat (satır kaymaları ~180 satır); indeks yenilenmeli (`node .gitnexus/run.cjs analyze`).

## Önerilen Öncelik Sırası (fix turu için)

1. **B2 + B3 + B22** (aynı alt sistem: modal router tutarlılığı — üçü bir arada düzeltilmeli)
2. **B4** (tek helper + mekanik değişim; gece veri bütünlüğü)
3. **B12 + B13 + B14 + B15** (forms.js veri-doğruluğu paketi — küçük, cerrahi yamalar)
4. **B16 + B17 + B18** (api.js senkron katmanı paketi)
5. **B5** (loadRaporlar tek fonksiyon), **B11** (çift tanım silme), **B8/B9/B10** (pattern taramaları)
6. **B7 canlı teyidi** (pg_get_functiondef — kullanıcı onaylı, yazma içermeyen okuma) + **GT yeniden üretimi**
7. **B1** (ürün kararı gerektirir: demo veri maskeleme)

---

*Rapor salt-okunur denetimdir; hiçbir kod değişikliği yapılmadı. Satır numaraları 2026-08-31 itibarıyla working tree'ye göredir (commit bb4ea92 sonrası, dirty olmayan js/ dosyaları).*
