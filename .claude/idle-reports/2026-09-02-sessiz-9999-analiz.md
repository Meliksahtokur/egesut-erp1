# idle/sessiz-9999-analiz — stat_suru_ozet ↔ sessiz liste sayım tutarsızlığı

**Tarih:** 2026-09-02 · **Worktree:** `/home/melik/egesut-wt/sessiz-9999-analiz` · **Taban:** main `a71b7a0`
**Guardrail:** PROD DB yalnız READ-ONLY (SELECT + pg_get_functiondef); migration TASLAK olarak repoda, DEPLOY EDİLMEDİ; js/ dokunulmadı; push yok.

---

## 1. Canlı ölçüm (orchestratör, 2026-09-02 — IDLE-GÖREV.md'den alıntı, yeniden türetilmedi)

| Ölçüm | Değer | Kaynak |
|---|---|---|
| `sessiz_hayvanlar_listele` satır sayısı | **24** | canlı RPC (COALESCE'li filtre) |
| `stat_suru_ozet → hayvan.sessiz` | **18** | canlı RPC (COALESCE'siz filtre) |
| Fark = `sessiz_gun` NULL hayvan | **6** | v_eligible |
| NULL hayvan küpe no'ları | **204, 2044, 207, 23, 4019, 906** | canlı SELECT |
| NULL hayvan profili | hepsinde `dogum_tarihi` NULL, 0 tohumlama, 0 doğum; 4 Düve (Büyük), 1 Sağmal Laktasyonda (2044), 1 Sağmal Kuru (906) | canlı SELECT |

**Kök neden (canlı pg_get_functiondef ile 2026-09-02'de doğrulandı):**
`stat_suru_ozet` gövdesinde `'sessiz', (SELECT COUNT(*) FROM public.v_eligible e WHERE ... AND e.sessiz_gun >= 55)` — **COALESCE yok**, NULL'ları saymaz. `20260614000002_stat_suru_ozet_v3.sql`'de ifade `COALESCE(e.sessiz_gun, 9999) >= 55` idi; canlıyı yeniden yazan `20260625000030_stat_suru_ozet_readonly.sql` yazılırken COALESCE düşmüş. Liste `COALESCE(...,9999) >= p_min_gun` kullandığından NULL'ları 9999 olarak DAHİL ediyor.

## 2. Zincirin kalan halkaları — katman katman NULL/9999 davranışı

`sessiz_gun` v_eligible'da CASE ile üretilir: son üreme event'i (kızgınlık/tohumlama/abort/doğum) → son doğum → `dogum_tarihi + 13 ay` (düve ankrajı, 20260831000002) → **ELSE NULL** (ne event'i ne doğum bilgisi = "hiç kayıt yok"). v_eligible WHERE'i zaten "son event'i 55 günden yeni olmayan"ları eler; NULL yalnızca üçü de boş olan hayvanlarda oluşur.

| Katman | İfade | NULL davranışı | Etki |
|---|---|---|---|
| `v_eligible` (view) | `CASE ... ELSE NULL` → `sessiz_gun` | NULL üretir (6 hayvan) | Kaynak katman; filtre yok |
| `sessiz_hayvanlar_listele(p_padok, p_min_gun)` | `WHERE COALESCE(e.sessiz_gun, 9999) >= p_min_gun` + çıktıda `sessiz_gun = COALESCE(...,9999)` | **DAHİL** (9999) | Liste 24; UI "Hiç kayıt yok" etiketi + en alta sıralama (bb4ea92) |
| `sessiz_hayvanlar_reconcile()` | Görev üretim: `WHERE e.sessiz_gun >= 55` · Kapatma: `NOT EXISTS (... e.sessiz_gun >= 55)` | **HARİÇ** (NULL >= 55 → NULL → false değil, bilinmez = satır dönmez) | 6 NULL hayvana vet-kontrol görevi ÜRETİLMEZ; açık SESSIZ- görevleri varsa `kapatan_ref='sessiz-noteligible'` ile KAPATILIR |
| `stat_suru_ozet` → `'sessiz'` | `WHERE ... e.sessiz_gun >= 55` | **HARİÇ** | Kart 18 (bugün) |
| pg_cron `sessiz-reconcile-daily` (jobid 7, `0 5 * * *`, active) | `SELECT public.sessiz_hayvanlar_reconcile()` | — | Her sabah 05:00'te reconcile'u çalıştırır → NULL hayvanlar sistematik olarak görevsiz kalır (yukarıdaki HARİÇ davranış) |
| `sessiz_hayvanlar_gorev_olustur()` | v3 döneminde `stat_suru_ozet` içinden çağrılıyordu; readonly fn (20260625000030) yazma yan etkisini kaldırdı | — | Bugün canlıda çağıran fn yok; görev üretimi tek makas = cron + reconcile |
| JS | `js/ui.js:309-310` → `rpc('sessiz_hayvanlar_listele')` → `_dashBands` (band başlığı `sessizList.length`) · `js/ui.js:1109` → `_showSessizList` modal · `js/ui.js:1662` → `rpc('stat_suru_ozet')` → `js/ui.js:1747-1748` `h.sessiz` → "Sessiz Hayvanlar (18)" bölümü | Client-side 9999 ayrımı (`s.sessiz_gun>=9999`) | Aynı ekranda band "24", kart "18" |

**Zincir özeti:** View (NULL üretir) → liste COALESCE'li (NULL dahil) → stat COALESCE'siz (NULL hariç) → reconcile COALESCE'siz (NULL hariç + görev kapatır) → cron her gün reconcile'u çalıştırır. Tutarsızlık tek satırlık ifadelerden; view ve veri doğru.

## 3. Blast radius

### Canlı DB (pg_proc/pg_views/pg_matviews gövde taraması + cron, 2026-09-02)

| Sorgu | Sonuç |
|---|---|
| `pg_views` gövdesinde `stat_suru_ozet` geçen view | **0** |
| `prosrc`'sünde `stat_suru_ozet` geçen diğer fonksiyon | **0** |
| `v_eligible`'ı okuyan fn'ler | Tam 3: `sessiz_hayvanlar_listele(text,integer)`, `sessiz_hayvanlar_reconcile()`, `stat_suru_ozet(text,boolean)` |
| `v_eligible`'ı okuyan view | **0** |
| pg_cron | jobid 7 `sessiz-reconcile-daily` → reconcile (etkilenmez; taslak onu değiştirmez) |

`pg_depend` notu: plpgsql fn gövdeleri katalog bağımlılığı üretmez (opak metin), bu yüzden doğruluk için `prosrc ILIKE` gövde taraması yapıldı — `stat_suru_ozet`'i okuyan başka DB nesnesi yok.

### GitNexus

- `mcp__gitnexus__impact({target:"stat_suru_ozet", direction:"upstream", repo:"egesut-erp1"})` → "Target not found" (index JS sembolleri; DB fn kapsam dışı — beklenen).
- `mcp__tools-bank__gitnexus_impact` → `/root/egesut-erp1` Permission denied (server kendi kök yoluna bakıyor; bu makinede `/home/melik/egesut-erp1`). DB tarafı zaten yukarıdaki katalog taramasıyla eksiksiz kapandı; JS tarafı aşağıda grep ile.

### JS tüketicileri (worktree grep)

| Yer | Kullanım | Taslağın etkisi |
|---|---|---|
| `js/ui.js:1662` (`_fetchSuruStat`) | `db.rpc('stat_suru_ozet', {p_son_donem, p_padok?})` — tek çağıran | Çıktı şeması DEĞİŞMİYOR (yalnız `hayvan.sessiz` değeri 18→24); JS değişikliği gerekmez |
| `js/ui.js:1747-1748` (`_applySuruStatHtml`) | `h.sessiz` → "❗ Sessiz Hayvanlar (N)" bölümü, alt yazı "55+ gündür tohumlama/kızgınlık kaydı yok", "Listeyi gör →" `_showSessizList`'e bağlanır | N 18→24; `_showSessizList` zaten 24 satır gösterdiği için kart-liste iç tutarlı hale gelir. Alt yazı metni 6 hayvan için tam teknik doğru değil ("hiç kayıt yok" durumu) — rapor düzeyinde not; değişiklik önerilmez (js/ dokunma kuralı) |
| `js/ui.js:196-244` (`_dashBands`) | band başlığı `sessizList.length` (24) + top-8 + "Hiç kayıt yok" etiketi + 9999'lar en alta | Değişmez; kartla eşleşir |
| `js/ui.js:1109-1118` (`_showSessizList`) | tam liste modalı (24 satır) | Değişmez |

Risk seviyesi: **LOW** — tek fn, tek sayısal alan; tüketiciler sadece sayıyı görüntülüyor, ayrıştırmıyor; şema/anahtar ekleme/çıkarma yok; reconcile ve cron dokunulmuyor.

## 4. Karar matrisi (bugünkü 24/18/6 ile)

| | (a) stat'a COALESCE restore | (b) ayrı `kayitsiz` sayacı + UI bandı ayrımı | (c) statü quo + veri tamamlama operasyonu |
|---|---|---|---|
| DB değişikliği | tek satır, v3'te kanıtlanmış ifade | fn gövdesine yeni anahtar + ikinci subquery | yok |
| JS değişikliği | **gerekmez** | `ui.js:1747` + `_dashBands:238` koordineli değişiklik + deploy → bu görevin kapsamı dışı, test yüzeyi büyür | yok |
| Kart/band/modal tutarlılığı (bugün) | 24 = 24 = 24 ✓ | kart "sessiz 18 + kayıtsız 6" = band 24 (JS şart) | **18 ≠ 24** devam eder |
| Semantik doğruluk | 6 hayvan "55+ gün sessiz" olarak SAYILIR; ama liste ve band zaten onları sayıyor ve UI "Hiç kayıt yok" diye dürüstçe etiketliyor + en alta atıyor | En dürüst: gerçek sessiz ölçümü (18) ile kayıt eksiğini (6) ayırır | Kart kafa karıştırıcı kalır |
| Veri eksiği görünürlüğü | Liste/modal üzerinden korunur (9999 etiketi) | En iyi | En iyi (liste zaten gösteriyor) |
| Gelecek risk | Yeni "hiç kayıt yok" hayvanlar otomatik tutarlı sayılır | Her yeni hayvan iki sayaca doğru düşer | Tutarsızlık her yeni hayvanda tekrar eder; operasyon disipline bağlı |
| Deploy maliyeti | 1 fn CREATE OR REPLACE, içerik-harmli (read-only fn), anlık | 1 fn + JS sürümü senkronu | 0 kod, ama insan işi |
| Yan etki | reconcile NULL'ları hâlâ HARİÇ tutar → NULL hayvanlara görev üretilmez (istenen: veri hayaletlerine vet-kontrol spam'i yok) | Aynı | Aynı |

### Öneri: **(a) — stat'a COALESCE'i geri koy**

Tek cümle gerekçe: Listele/band/modal zaten NULL'ları 9999 olarak sayıp "Hiç kayıt yok" etiketiyle dürüstçe gösterdiği için ekran bütünlüğünü tek satırlık, v3'te kanıtlanmış, JS'siz restore sağlar; (b)'nin sağladığı "kayıtsız ayrımı" bilgisi mevcut UI'da zaten var ve (b) js/ + DB senkron deploy gerektirir, (c) ise ekrandaki çelişkiyi sonsuza kadar bırakır.

**Bilinçli asimetri notu:** `reconcile`'in NULL'ları hariç tutması (görev üretmez, açık görevleri kapatır) taslakla DEĞİŞTİRİLMEDİ — veri eksiği olan 6 hayvana otomatik vet-kontrol görevi açılması istenmez; önce `dogum_tarihi`/yaş verisinin girilmesi gerekir. İstenirse ayrı bir görevde "kayıtsız hayvan veri tamamlama görevi" düşünülebilir.

## 5. Migration taslağı

`supabase/migrations/20260902000000_stat_suru_ozet_sessiz_coalesce.sql` — mevcut pattern'e uygun tam gövde `CREATE OR REPLACE`; `_revize/_fix` yok. Kanıt: taslak, `20260625000030_stat_suru_ozet_readonly.sql` ile diff'lendi — tek gövde farkı:

```diff
-    'sessiz', (SELECT COUNT(*) FROM public.v_eligible e WHERE (p_padok IS NULL OR e.padok = p_padok) AND e.sessiz_gun >= 55),
+    'sessiz', (SELECT COUNT(*) FROM public.v_eligible e WHERE (p_padok IS NULL OR e.padok = p_padok) AND COALESCE(e.sessiz_gun, 9999) >= 55),
```

(gövde canlı `pg_get_functiondef('stat_suru_ozet')` çıktısıyla da birebir; diğer farklar yalnız başlık yorumu ve sondaki `;`.)

Beklenen sonuç deploy sonrası: `stat_suru_ozet → hayvan.sessiz` 18 → **24** = `sessiz_hayvanlar_listele` uzunluğu = dashboard bandı. **TASLAK — deploy kullanıcı emriyle.**

## 6. Teslim durumu

- [x] Zincir halkaları tablosu (v_eligible, listele, reconcile, stat, cron, JS)
- [x] Karar matrisi + tek öneri (a)
- [x] Migration taslağı (repoda, deploy edilmedi)
- [x] Blast radius (canlı katalog + JS grep + gitnexus denemeleri)
- [ ] Deploy (kullanıcının sonraki oturumda vermesi gereken emir)
- js/ ve ana repo dokunulmadı; tek commit, push yok.
