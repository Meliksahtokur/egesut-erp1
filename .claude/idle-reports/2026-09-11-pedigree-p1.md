# Teslim raporu — G-20260911-PEDIGREE-P1-TEMEL (P1 "Temel" paketi)

**Dal:** `idle/pedigree-p1` @ **033704e** (main aa075c8'in ilerisinde; TABAN-TAM doğrulandı)
**Tarih:** 2026-09-11 · **Lead:** ss-lead/pedigree-p1 (GLM) · **Worker'lar:** W1/W2/W3 (GLMF) + review tur-1 (Codex luna max)
**Kanıt zinciri:** kırıntılar `.ss/pedigree-p1-crumbs.jsonl` (lead) + `~egesut-erp1/.crumbs/pedigree-p1-w*.jsonl` (worker'lar); review raporu `.claude/reviews/2026-09-11-pedigree-p1-lead-review.md`

## Kabul kriterleri — kanıt satırları

### 1. G0b — `dogum.buzagi_id` foundation ✅
- `psql $DATABASE_URL -f tests/sql/dogum_buzagi_id_test.sql` → `W1T: TUM TESTLER GECTI`, EXIT=0
  (T1 dogum_kaydet aynı-transaction yazar; T2 unique ihlali 23505 reddi; T3 calf DELETE → dogum kalır + buzagi_id NULL; T4b-e dört backfill sınıfı)
- Backfill sayaçları (demo 72 satır): **auto=70, cok-aday=1, tarih-uyumsuz=0, aday-yok=1**; replay 2. koşum auto=0
- Migration: `supabase/migrations/20260911000001_dogum_buzagi_id_foundation.sql` (review F6 düzeltmesi: iç BEGIN/COMMIT çıkarıldı — 61e2acd)

### 2. G1 — pedigree foundation ✅ (review tur-1 düzeltmeleriyle)
- `psql $DATABASE_URL -f tests/sql/pedigree_graph_test.sql` → `TESTDONE — 19 blok yeşil`, EXIT=0
  (10 invariant + sex normalize + operatör guard üçlüsü + grant denetimleri + F1/F2/F3 fixture'ları)
- `supabase/migrations/20260911000002_pedigree_foundation.sql`: nodes/parentage/semen_catalog/meta + assert_is_operator + 6 helper + trigger; RLS USING(true); grant'lar tam imzayla kendi migration'ında
- **Review tur-1** (codex luna max, 84f3879): 6 bulgu tümü demo-kanıtlı VERIFIED → F1-F6 aynı worker'lara döndü → düzeltmeler 61e2acd + 35c4c57 → mekanik kapı 19 blok yeşil. F4 lead kararı: `dogum_kaydet`→core ÜRETİM bağlantısı Faz 7'dir (plan Task 0.4 + ET owner kapısı); P1'de test yalnız INTERNAL core mekanizma kanıtıdır.

### 3. G2 — backfill + integrity report ✅
- `psql $DATABASE_URL -f tests/sql/pedigree_farm_backfill_test.sql` → `TUM KONTROLLER GECTI`, EXIT=0 (11 bölüm: F0-FH)
  - Fixture kapsamında **blockers==0** (temiz fixture delta 0)
  - 18-kod emisyon kuralları: bulunmayan grup emit edilmiyor; cutoff `tanimsiz` → post-cutoff grubu atlanıyor; bozuk cutoff `'1900-13-45'` → istisna değil `gecersiz` (blocker); Task 8 bekleyen gruplar emit edilmiyor (FF)
  - Maternal edge yalnız `buzagi_id` FK predikatıyla (kupe-join yok); idempotency: 2. koşum 0/0 (FA2/FB2)
- **Gerçek sürü ölçümü** (lead canlı, 2026-09-11): 166 node + 59 maternal edge; cutoff=tanimsız;
  `maternal_tarihsel_uyumsuz` blocker×2, `child_without_dam` warning×13, `child_without_sire` info×166, `dogum_buzagi_missing` info×2 — veri kalitesi bulguları, raporün tasarlanan çıktısı (plan Task 0.3 öngörüsü)
- Migration: `supabase/migrations/20260911000003_pedigree_farm_backfill.sql` (replay 0/0; grant kendi migration'ında)

### 4. Unit suite ✅
- `npm run test:unit` → **737 test / 736 pass / 1 fail** — tek kırmızı goal'ün belgelediği known-red `tests/unit/gecmis-pipeline.test.js:283` (DÜN testi, main'de de kırmızı); tabanla aynı, yeni kırmızı yok. Lead, birleşik dal @ 033704e'de yeniden koştu.

### 5. Migration disiplini ✅
- Replay-safe idempotent DO blokları (üç migration'da da 2. koşum no-op ölçüldü)
- `farm_id DEFAULT '400b9107-…'` + farm_id-öncülü indexler; RLS USING(true); her grant nesneyi yaratan migration'da; helper imzaları plan 1.4 ile birebir
- Yetki sızdırmazlığı (review sonrası): `semen_catalog` authenticated SELECT-only; SD helper'larda PUBLIC dahil tam REVOKE; anon'a hiçbir şey

### 6. Bu rapor + kırıntı disiplini ✅
- Kırıntılar: lead `.ss/pedigree-p1-crumbs.jsonl`; worker kırıntıları `~egesut-erp1/.crumbs/pedigree-p1-w{1,2,3}.jsonl`; BOARD `.ss/pedigree-p1-BOARD.md` güncel

## Dal grafiği

```
main aa075c8 ─┬─ d7df5c2 (W1/W2 zarfları)
              ├─ 311d1d6 merge W1 (ccb22ed Task 0.5)
              ├─ e1a825d merge review tur-1 (84f3879, 6 bulgu)
              ├─ ec3516c merge W2 (5d26aea Task 1)
              ├─ 9aa7048 merge W1-F6fix (61e2acd)
              ├─ 8e4d490 merge W2-F1F6fix (35c4c57)
              └─ 033704e merge W3 (980efb4 Task 2)  ← DAL UCU
```

## Açık kalemler (root/owner kapıları)

1. **Deploy:** üç migration commit'li ≠ deploy; demo'ya uygulandı, **PROD owner kapısı**.
2. **Dry-run kapısı P1'de pedigree zincirini koşturamaz** (W3 ölçümü): Neon aynası PROD kaynaklı + constraint-free → W1 FK'si 42830 düşer; foundation PROD'a inene kadar kabul yolu demo-tx probe + demo apply (plan 1.6).
3. **Gerçek sürü veri kalitesi:** 2 blocker (kupe55 `5979186d` tarih-uyumsuz; kupe31 `982fbd16` doğum kaydı yok) + 13 child_without_dam — sahibin düzeltme kararını bekler (P1 dışı).
4. **`uq_hayvanlar_kupe_no` FULL unique** — teklif Rev 2 "aktif+dolu benzersizlik" varsayımıyla uyumsuz (doc-drift; W1 ölçümü; backfill doğru davranıyor).
5. **GT regen** (`99999999999999_ground_truth.sql`): deploy sonrası ayrı root adımı (plan SMELL-003).
6. **ET owner kararı:** Faz 7 (doğum parentage write) önkoşulu — `dogum_kaydet`→core üretim bağlantısı orada.
7. **Semen mapping owner review:** Task 8/P3 kapısı (taslak işlendi: `.claude/specs/2026-09-10-pedigree-semen-mapping.md`).
8. **Task 1.7 write_manifest gerilimi:** `scripts/db-dry-run.sh` + `refresh_lsp_schema.sh` + README satırı + `.gitignore` negasyonları plan Task 1.7 gereği dahil edildi; goal write_manifest'te yoktu (lead kararı, root merge kapısında).
9. **ss-dispatch çapraz-repo taban kapısı kusuru:** script repo'su (tools-bank) denetliyor; egesut dağıtımında exit 6 ×2 — dağıtım `superset workspaces create` primitivesiyle yapıldı (dallar önceden sabit + HEAD doğrulandı). tools-bank fix'i root'a.
10. **Repo'da `.superset/config.json` yok:** workspace'lere .env/node_modules taşınmıyor; symlink bootstrap kullanıldı — root setup skill'i ile kapatılabilir.
11. **Demo DB paylaşımlı:** eşzamanlı aktör churn'ü gözlendi (W1); sayaçlar koşum anı için geçerli.
12. `refresh_lsp_schema.sh` worker'larca ÇALIŞTIRILMADI (PROD Mgmt API okur — goal durdurma koşulu); owner çalıştırabilir.

## Teslim ölçütü (goal zarfı)

`idle/pedigree-p1` main'in ilerisinde; W1+W2+W3 (+review tur-1 + düzeltmeler) merge'li; bu rapor dalda. **Dal root kapanışına açık bırakılır** (lead dalları silmez).
