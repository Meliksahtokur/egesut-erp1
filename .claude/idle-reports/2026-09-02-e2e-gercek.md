# Idle Raporu — e2e-gercek: E2E Gerçek-Suite Onarımı (2. jenerasyon)

**Tarih:** 2026-09-02 · **Worktree:** `/home/melik/egesut-wt/e2e-gercek` (branch `idle/e2e-gercek`, taban e6d8782)
**Koşum modu:** GERÇEK demo modu (`PLAYWRIGHT_DEMO_MODE=1`, stub'sız) — DNS kontrolü `getent hosts vtzqjmazsvurxdeondmi.supabase.co` çözündü (104.18.38.10). Docker: `mcr.microsoft.com/playwright:v1.58.2-noble`, `PLAYWRIGHT_BASE_URL=http://127.0.0.1:8080/` (webServer python http.server container içinde). node_modules symlink'i docker'a `/home/melik/egesut-erp1/node_modules` ayrı mount'u ile çözüldü.

## Sonuç özeti

| Koşum | Komut | Sonuç |
|---|---|---|
| run1 (pilot) | smoke tekil, paralel | 6 pass / 3 fail — UI drift + ortam hataları görüldü |
| run2 | TÜM suite, `--workers=1` | 52 pass / 14 fail / 1 skip (11.6 dk) |
| run3 | TÜM suite, `--workers=1` | 60 pass / 4 fail / 3 skip (7.1 dk) |
| run4 | TÜM suite, `--workers=1` | 62 pass / 2 fail (3.01/3.02) / 3 skip (8.1 dk) |
| **run5 (final)** | TÜM suite, `--workers=1` | **64 passed / 0 failed / 3 skipped (6.2 dk) — YEŞİL** |

Final koşum komutu (tek komut):

```
docker run --rm -e PLAYWRIGHT_DEMO_MODE=1 -e PLAYWRIGHT_BASE_URL=http://127.0.0.1:8080/ \
  -v "$PWD":/work -v /home/melik/egesut-erp1/node_modules:/home/melik/egesut-erp1/node_modules \
  -w /work mcr.microsoft.com/playwright:v1.58.2-noble \
  npx playwright test tests/e2e.spec.js tests/smoke.spec.js tests/sablon.spec.js \
    tests/sutten-kes.spec.js tests/modal-router.spec.js tests/kritik-akis.spec.js \
    tests/gece-tarih.spec.js tests/offline-kuyruk.spec.js --workers=1 --reporter=list
```

## 1. navTo çift-# bug (İş 1) — KÖK NEDEN

`tests/e2e.spec.js`'in **yerel** `navTo`'su `'#' + btnId.replace('nb-','pg-')` üretiyordu; `btnId` zaten `#` önekli geçtiğinden selektör `'##pg-suru.on'` oluyor, CSS parse hatası veriyordu → `navTo` çağıran ~30 test düşüyordu. `tests/support/app.js`'teki paylaşılan `navTo` doğru yazılmıştı (`#`'i önce soyuyor) — 1. tur spec'leri (modal-router/kritik-akis/gece-tarih/offline-kuyruk) etkilenmedi. Aynı hata test 6.04'te satır içi de vardı.

**Fix:** e2e.spec.js yerel `navTo` ve 6.04 satır içi pattern'i `btnId.replace(/^#/,'')` ile # soyacak şekilde düzeltildi (support/app.js ile aynı mantık). Diğer spec'lerde tarama yapıldı: `grep "replace('nb-'"` → yalnızca e2e.spec.js'de bulunuyordu; smoke/sablon/sutten-kes navTo hiç kullanmıyor (doğrudan `#nb-*` click + açık sayfa selektörü).

## 2. Eski 4 spec'in demo-mode uyumu (İş 2)

### Ortam bulguları (DB tarafı — kod değişikliği YAPILMADI, guardrail gereği)

| # | Bulgu | Kanıt | Etki |
|---|---|---|---|
| O1 | **Demo projesinde anon rolün tablo okuma yetkisi yok** — 2026-09 restore sonrası GRANT kaybı | `GET /rest/v1/hayvanlar` anon → 401/`42501 permission denied for table hayvanlar`; authenticated (demo@egesut.web) → 200 + veri | UI demo autologin ile çalışır; test DB client'ları (anon) kırılıyordu |
| O2 | **Demo şeması eski** — UI uyarısı: "Şema drift: prod'a 3 tablo / 0 kolon eklenmiş, demo eskimiş. Migration'ı demo'ya uygula + FDW yenile." | `POST /rpc/demo_sema_diff` → 401 (RPC yok/eski); dashboard stat'leri hepsi 0 (FDW/eski şema) | `tohumlama_sonuc_gebe` gerçek kayıtla `ok:false` + mesajsız → UI "❌ Sonuç kaydedilemedi: İşlem başarısız" |
| O3 | `rpc/hekim_listesi` demo'da yok | 404 (bilinen eksik — fix-oturumu 2026-08-31 memory'sinde de kayıtlı) | Konsol 404 hatası |
| O4 | **index.html:2153 `<script src="agent-telemetry/tracker.js">` commit'lenmiş, dosya repoda yok** | Her sayfa yüklemesinde `GET /agent-telemetry/tracker.js` 404 (GH Pages dahil her ortamda) | Konsol "Failed to load resource" — kritik-hata assertion'ları kırılıyordu. **js/index.html fix yasak → BULGU olarak raporlanıyor; testler location-bazlı filtre ile bağışıklandırıldı. Bu tag'in kaynağı/ayrılması ayrıca incelenmeli.** |

### Test tarafı düzeltmeler (yalnız `tests/`)

**tests/e2e.spec.js:**
- navTo çift-# fix + 6.04 satır içi (yukarıda).
- `IGNORED_LOCATIONS` filtresi (console hatası **metni URL içermediğinden** `msg.location().url` ile): `agent-telemetry`, `rpc/demo_sema_diff`, `rpc/hekim_listesi` — adet bazlı, genel "Failed to load resource" maskesi bilinçli YOK (gerçek regresyonları örtmesin).
- **DB client'ları demo oturumuyla okur** (`ensureSession` → `signInWithPassword(DEMO_LOGIN)`, worker başına 1 kez cache'li; O1'e çözüm; CI'nın demosuz prod koşusu anon okumayla sürer).
- 1.01: openApp sonrası redundant `.sv`/`#pg-dash` expect'leri çıkarıldı — openApp zaten 30s'de görünür olduğunu doğruluyor; demo autologin reload + gecikmeli pull'da toplam 30s test sınırı aşılarak yanlış-negatif üretiyordu (fail mesajı `.sv` bekleyişi gibi görünüyordu; gerçekte test timeout kesmesi).
- 2.03: kart küpesi ↔ detay ↔ DB üçgeni (`.a-id` etiketi + `dbGetByKupe`); `#det-name` async dolduğundan poll'lu. REST default sırası ≠ UI kart sırası olduğu için eski "dbGetFirst ile ilk satır" karşılaştırması yanlış-negatif üretiyordu.
- 3.01/3.02: diseases dropdown'u **pullTables'in seri `_pullChain` zincirinin sonlarında** (30+ tablo sırayla; dash render sonrası 3-6s, yüklü koşumda 25s+) doluyor — anlık count race üretiyordu; **arrange**: modalı açmadan önce uygulamanın kendi `pullTables(['diseases'])` çağrısıyla faz öne alınır (yalnız okuma) + poll 30s + `test.setTimeout(90000)`. (Ampirik: `GET /rest/v1/diseases` 200, IDB'ye 44 kayıt, dropdown 45 option.)
- 3.03/3.05: `#tab-*` id'leri görünmez `.tab-pane` içerik paneli — tıklama hedefi `button[data-action="tab-*"]` (UI refactor drift). 3.06'daki tabs dizisi de aynı sebeple düzeltildi.
- 3.04: `text=İptal` tüm modal sheet'lerinde geçiyor, ilk DOM eşleşmesi gizli modal olabilir → `#m-disease [data-action="close-disease"]`.
- 4.02: "Kızgınlık Kaydı" butonu log sayfasında YOK (refactor'la taşındı) → üreme sayfası kızgınlık tab'ındaki "🔴 Kızgınlık Ekle" butonu (ui.js:2628, koşulsuz render) üzerinden.
- 4.04: `text=Tohumlama` üreme tab'ıyla da eşleşiyor (ilk DOM eşleşmesi gizli) → `[data-action="open-insem-modal"]`.
- 5.04: `.mo.on, .modal` first() gizli sheet'e takılıyor → `#m-task-add` `.on` class doğrulaması.
- 5.05: global `.fs-btn` ilk eşleşmesi üreme sayfasının gizli tab'ı → `#pg-tasks .fs-btn` kapsamı.
- STUB guard'ları: 2.01/2.03/3.02 stub modunda skip (UI fixture'ı ile gerçek demo DB doğrulaması eşleşmez).

**tests/smoke.spec.js:** aynı `IGNORED_LOCATIONS` filtresi; detay tab testi `data-action` butonlarına; FAB testi sürü→görevler sayfasına (FAB `#pg-tasks` içinde, index.html:694) + modal `#m-task-add .modal` görünür + kapanış doğrulaması.

**tests/sutten-kes.spec.js:** veri-agnostic rewrite — `openSuttenKesModal` süt içen buzağı yoksa modalı hiç açmayıp toast basıyor (forms.js:782). Test artık iki dalı da doğruluyor: buzağı VARSA modal+picker+ tarih default; YOKSA modal kapalı + "Süt içen buzağı yok" toast'ı.

**tests/kritik-akis.spec.js:** `tohumlama sonucu Gebe` ve `B20` testleri gerçek demo modunda **gerekçeli skip** (O2: demo `tohumlama_sonuc_*` RPC'leri eski sürüm; ölçüm: sahte UUID → `{"ok":false,"error":"Tohumlama bulunamadı"}` fn VAR, ama gerçek kayıtla mesajsız `ok:false` → UI toast hata). Stub modunda (`PLAYWRIGHT_STUB_BACKEND=1`) koşmaya devam ederler. **Demo projesine eksik migration'lar uygulanınca bu iki skip kaldırılmalı.**

**tests/support/app.js:** `DEMO_LOGIN` ile `demoSession()` (promise cache'li oturum); `gorevByMarker`/`gorevReachedBackend` authenticated okur (O1).

## 3. TÜM suite koşumu (İş 3)

8 spec tek komutta, `--workers=1` (CI ile tutarlı; **run1'de 6 paralel worker ile 23 fail** aldık — demo free-tier rate-limit/latency paralel yükü kaldırmıyor; workers=1 ile kararlılaştı. Bu aynı zamanda CI'da `process.env.CI` zaten workers=1 kullanıyor).

**Final (run5): 64 passed / 0 failed / 3 skipped — tek komutla yeşil.**

| Spec | Test | Sonuç |
|---|---|---|
| tests/e2e.spec.js | 43 | 43 pass |
| tests/smoke.spec.js | 9 | 9 pass |
| tests/modal-router.spec.js | 5 | 5 pass |
| tests/kritik-akis.spec.js | 3 | 1 pass + **2 skip** (demo `tohumlama_sonuc_*` RPC eski sürüm — O2; migration sonrası açılmalı) |
| tests/gece-tarih.spec.js | 3 | 2 pass + 1 skip (3. test: clock'lu sayfada `animals` pull'u race — veri-agnostic `!anneKupe` skip dalı) |
| tests/offline-kuyruk.spec.js | 2 | 2 pass (gerçek demo DB'ye yazma → kuyruk/replay/B17 zinciri canlı doğrulandı) |
| tests/sablon.spec.js | 1 | 1 pass |
| tests/sutten-kes.spec.js | 1 | 1 pass (iki-dal veri-agnostic) |

## 4. CI önerisi (İş 4 — kod YOK, yalnız öneri)

Mevcut `test.yml`: 3 shard + smoke job, demosuz, baseURL=prod GH Pages. Öneriler:

1. **Stub-backend fallback adımı** (demo pause'a karşı dayanıklılık — 2026-09-01'de yaşanan NXDOMAIN senaryosu):
   ```yaml
   - name: Demo health check
     id: demo
     run: |
       code=$(curl -s -o /dev/null -w '%{http_code}' --max-time 10 \
         "https://vtzqjmazsvurxdeondmi.supabase.co/rest/v1/" \
         -H "apikey: ${DEMO_ANON_KEY}")
       echo "code=$code" >> "$GITHUB_OUTPUT"
   - name: Run E2E (demo ayıksa)
     if: steps.demo.outputs.code != '000'
     env: { PLAYWRIGHT_DEMO_MODE: '1' }
     run: npx playwright test ... --workers=1
   - name: Run E2E (stub fallback)
     if: steps.demo.outputs.code == '000'
     env: { PLAYWRIGHT_DEMO_MODE: '1', PLAYWRIGHT_STUB_BACKEND: '1' }
     run: npx playwright test ... --workers=1
   ```
   Stub modunda e2e.spec'in DB↔UI testleri (2.01/2.03/3.02) bu commit'le gelen guard'larla otomatik skip (log'da gerekçe görünür).
2. **Demo gerçek koşumu tek job + `--workers=1`** yapın; shard'lar aynı free-tier demo projesine paralel vurur (run1 kanıtı: paralel=23 fail, serial=kararlı).
3. smoke job demosuz prod koşusuna devam edebilir; O4 (agent-telemetry 404) ve O3 (hekim_listesi) console filtreleri test tarafında çözüldü — CI'da ek adım gerekmez.
4. Opsiyonel: haftalık scheduled demo-health job; pause tespitinde issue aç → restore runbook (session-learnings 2026-09-01).

## 5. Teslim

- Tek commit: `idle: e2e-gercek — navTo fix + eski suite demo-mode onarımı` (tests/ + bu rapor).
- `docker run --rm -v $PWD:/wt alpine rm -rf /wt/test-results` ile root-sahipli test-results temizlendi.
- Push YOK (sabah review).

## Açık kalemler (kullanıcı kararı bekler)

1. **O2 — demo projesine eksik migration'ları uygula** (3 tablo + FDW yenile). Uygulandığında: kritik-akis'teki 2 skip kaldırılmalı, dashboard stat 0'ları düzelmesi beklenir, `demo_sema_diff` 401'i kalkar (test filtresi zararsız kalır).
2. **O1 — demo'da anon GRANT'ları** restore edilmeli mi, yoksa "yalnız authenticated" bilinçli mi? Testler her iki durumda da çalışır (oturumlu okur); UI demo autologin'e bağımlı kalır.
3. **O4 — index.html'deki `agent-telemetry/tracker.js` tag'i** nereden geldi, kaldırılmalı mı? (js/index.html fix bu görevin kapsamı dışındaydı.)
