# Idle Raporu — demo-sync: Demo projesine eksik migration'lar + kritik-akis skip açılımı

**Tarih:** 2026-09-02 · **Worktree:** `/home/melik/egesut-wt/demo-sync` (branch `idle/demo-sync`, taban main `a71b7a0`)
**Kaynak:** IDLE-GÖREV.md (REV-1) — e2e-gercek raporu açık kalemleri O2 + skip notu
**Guardrail uyumu:** PROD'a karşı yalnız read-only SQL (`/database/query` + SELECT; prod token `tools-bank/.env SUPABASE_MANAGEMENT_TOKEN`, ref `zqnexqbdfvbhlxzelzju` — her çağrıda URL'den doğrulandı). Yazma YALNIZ demo (`vtzqjmazsvurxdeondmi`, token `.mcp.json supabase-demo`) — deploy yalnız `curl` + Management API `POST /v1/projects/vtzqjmazsvurxdeondmi/database/query`. tools-bank MCP supabase_* araçları demo'ya KULLANILMADI. Push yok, tek commit.

## Sonuç özeti

| Adım | Sonuç |
|---|---|
| 1. Envanter | Kolon drift'i: 5 ERP kolon · Fonksiyon: 16 eksik + 22 eski gövde · Tablo: ERP tarafı tam (fark yalnız bilinçli hariç 3 AI-infra) |
| 2. Deploy | **31 migration dosyası** (20260706000001 → 20260902000002) + 1 ön-adım — 32/32 OK |
| 3. FDW | `prod_fdw` refresh: 61 ft → **58 ft**; `demo_sema_diff()` → `{"eksik_tablo":[],"eksik_kolon":[]}` |
| 4. E2E | **67 passed / 0 failed / 1 skipped** (hedef 66/0/≤2 — geçildi; kalan 1 skip gece-tarih veri-race, gerekçeli) |

## 1. Envanter (prod vs demo, ölçüm yöntemiyle)

Karşılaştırma: `information_schema.columns` + `pg_class` + `pg_proc` (imza) + `md5(pg_get_functiondef)` gövde hash'i — iki tarafta da Management API `/database/query` üzerinden (prod read-only).

**İlk denemede string_agg tabanlı tablo listesi hatalı sonuç verdi (bildirim_log "yok" sandı)** — satır-bazlı `pg_class` sorgusuyla düzeltildi. Ders: katalog diff'lerinde aggregate değil satır seti kullan.

| Katman | Prod | Demo | Fark (ERP) |
|---|---|---|---|
| Tablo+view (r/v/m) | 61 | 59 (+demo_klon_log) | YOK — prod'daki 3 fazlalık `code_embeddings`, `entity_graph`, `memory_notes` (tools-bank AI-infra; FDW/klon'da bilinçli `EXCEPT`) |
| Kolon | 677 | 654 | 5 ERP kolon: `dogum.olay_id`, `drug_classes.etken_kod`, `tedavi_sablonu.tohumlama_plani`, `tohumlama.abort_tarihi`, `tohumlama.gerceklesme_at` (kalan 23 kolon farkı aynı 3 AI-infra tablosu) |
| Fonksiyon imzası | 189 | 183 | 16 eksik (14 ERP + `search_code`/`search_memory_notes` tools-bank/vector — demo'ya uygulanmaz); demo'da 3 prod'da olmayan legacy: `asi_gorev_planla`, `asi_planli_tamamla`, `fn_gorev_asip_iade` (hiçbir repo migration'ında yok — eski manuel deneme kalıntısı, dokunulmadı) |
| Fonksiyon gövdesi (aynı imza) | — | — | 22 fonksiyon md5 farklı (O2 dahil: `tohumlama_sonuc_gebe` eski sürüm) |

Demo'nun `supabase_migrations.schema_migrations` tablosu dump-kökenli (yalnız 2 kayıt: 20260706040737, 20260706052550 — islem_log immutable + asistan_sql invoker manuel hotfix'leri). Bu yüzden cutoff nesne-bazlı belirlendi: ilk eksik `20260706000003` (asistan_plan_iptal); 20260614 ve öncesi tam, 20260706000002/04 demo'da manuel uygulanmıştı.

## 2. Demo'ya uygulanan migration'lar (sırayla, içerik değiştirilmeden)

Ön-adım: `DROP TRIGGER IF EXISTS trg_islem_log_immutable ON public.islem_log;` — demo'daki manuel hotfix (20260706040737) 20260706000002'nin `CREATE TRIGGER`'ı (IF NOT EXISTS'siz) ile çakışacaktı; aynı içeriğin yeniden kurulumu için zorunlu. Migration dosyalarının İÇERİĞİ değiştirilmedi.

| # | Migration | # | Migration |
|---|---|---|---|
| 1 | 20260706000001_be_h1_tedavi_sil_islem_log | 17 | 20260730000002_vaka_tohumlama_ekle |
| 2 | 20260706000002_critical6_islem_log_immutable | 18 | 20260730000003_postpartum_e_vitamin_legacy_task_reconcile |
| 3 | 20260706000003_medium_be_fixes | 19 | 20260830000010_abort_vwp_penceresi |
| 4 | 20260706000004_m6_be_asistan_sql_invoker | 20 | 20260830000020_sessiz_vaka_filtresi_kaldir |
| 5 | 20260706000005_h14_gorev_guncelle_islem_log | 21 | 20260830000030_cift_abort_log_fix |
| 6 | 20260706000006_h11_hayvan_yas_grup_validasyon | 22 | 20260830000031_gebe_sonuc_mesaji |
| 7 | 20260710000001_protokol_etken_kod_authoritative | 23 | 20260830000032_geri_al_uuid_fallback |
| 8 | 20260717000001_hayvan_grup_padok_sync | 24 | 20260830000033_geri_al_hardening |
| 9 | 20260718000001_protokol_scanner_task_authority | 25 | 20260830000034_review_fix_paketi |
| 10 | 20260722000001_tedavi_sablon_gun_ofsetlerini_koru | 26 | 20260831000001_sessiz_ankra_fixi |
| 11 | 20260722000002_sablon_planli_tohumlama | 27 | 20260831000002_duve_sessiz_13ay |
| 12 | 20260722000003_planli_tohumlama_bagimsiz_event | 28 | 20260831000003_tablo_guard_triggerlari |
| 13 | 20260722000004_tohumlama_son_kayit_tarih_siralama | 29 | 20260901000001_ikiz_dogum_olay_id |
| 14 | 20260730000001_sablon_tohumlama_opsiyonel_ve_yasam_dongusu | 30 | 20260901000002_kupe_revizyon |
| 15 | 20260730000002_dogum_sonrasi_e_vitamini | 31 | 20260902000001_asi_stok_backfill |
| 16 | — (15 ve 16: repo'da 20260730000002 versiyon çakışması — İKİ dosya ayrı ayrı uygulandı) | 32 | 20260902000002_asi_stok_serbest_dusum |

Not: repo'da **20260730000002 versiyonu iki dosyada kullanılıyor** (dogum_sonrasi_e_vitamini, vaka_tohumlama_ekle) — pre-existing; Management API uygulaması version tablosu tutmadığından sorun yaratmadı ama CLI tabanlı bir pipeline'da patlar (ayrı kayıt, bu görevde düzeltme yok).

**Deploy doğrulaması (sonrası diff):**
- Kolon farkı (ERP): **YOK**
- Eksik fonksiyon (ERP): **YOK**
- Gövde hash farkı: 22 → **3** (aşağıda)

### Kalan 3 gövde farkı — BULGU

| Fonksiyon | Karakter | Açıklama |
|---|---|---|
| `get_vaccination_schedule` | Kozmetik | Prod'da satır-bazlı formatlanmış aynı mantık (whitespace). Anlamsal fark yok. |
| `_guard_tohumlama_yas_cinsiyet` | Kozmetik | Prod'da 1 yorum satırı fazlası. Anlamsal fark yok. |
| `gorev_tamamla` | **SEMANTİK — prod migration-dışı** | Prod'da ekstra guard: `AND v_gorev.gorev_tipi IS DISTINCT FROM 'ASI_PLANLI'` (ASI_PLANLI görevlerde generic stok düşümünü engelleyen çift-düşüm koruması). **Bu değişiklik repo migration'larında YOK** (`grep ASI_PLANLI supabase/migrations/` → 0 sonuç) — prod'a dosyasız hotfix olarak gitmiş. Demo'da repo-kanonik sürüm uygulandı; prod'la eşitlemek için bu hotfix'in REPO'YA migration olarak işlenmesi gerekir (bu görevin kapsamı dışı — migration dosyası yazma yetkisi yok). |

İlgili bulgu: demo'daki 3 legacy fonksiyon (`asi_gorev_planla`, `asi_planli_tamamla`, `fn_gorev_asip_iade`) da aynı aşı-görev akışı fix'inin parçaları olmalı — prod'da silinmiş, repoda hiç yokmuş. Prod ile repo arasında "migration dışı değişiklik" sürükleniyor; **öneri:** prod `pg_proc`'ü repo migration replay'iyle periyodik karşılaştırılıp farklar migration'a alınmalı (ground-truth-audit.sh'in DB-ayaklı bir versiyonu).

## 3. FDW refresh + dashboard stat

**Önce (drift'li durum, e2e-gercek raporu O2 + bugünkü ölçüm):** dashboard stat'leri 0; UI uyarısı "Şema drift: prod'a 3 tablo / 0 kolon eklenmiş". Eski `prod_fdw` import'u (61 ft) hem 3 AI-infra'yı içeriyor hem de prod'dan silinmiş 3 hayalet view taşıyordu (`_regen_fndefs`, `hayvan_durum_analizi`, `vethek_tohumlamalar`) — UI'daki "3 tablo" uyarısının gerçek kaynağı bu hayaletlerdi.

**Refresh (demo/01_fdw_bridge.sql prosedürü, şema katmanı):**
```sql
DROP SCHEMA IF EXISTS prod_fdw CASCADE;
CREATE SCHEMA prod_fdw;
IMPORT FOREIGN SCHEMA public EXCEPT (code_embeddings, entity_graph, memory_notes)
  FROM SERVER prod_srv INTO prod_fdw;
```
`prod_srv` server + `postgres` user mapping (demo_reader şifresi) korundu — şifre gerektiren server yeniden kurulumu gerekmedi.

**Sonra:**
- `information_schema.foreign_tables` (prod_fdw): 61 → **58** (EXCEPT'in 3'ü düştü)
- `SELECT demo_sema_diff();` → `{"eksik_kolon":[], "eksik_tablo":[]}` → UI drift uyarısı kalktı
- FDW canlı okuma kanıtı: `SELECT count(*) FROM prod_fdw.hayvanlar` → **166** (demo public: 164 — klon yaşı farkı, normal)
- `stat_suru_ozet(NULL, true)` → dolu: `toplam:141, duve:30, inek:69, erkek:26, buzağı:41, sessiz:18, gebe:77` + gebelik deneme dağılımı (d1: 33/39 … d7: 1/1) — **stat'ler 0'dan döndü**
- O2 kök doğrulaması: `tohumlama_sonuc_gebe('00000000-…')` → `{"ok":false,"mesaj":"Tohumlama bulunamadı"}` (mesajlı); gerçek Bekliyor kayıt (`ae015793-…`) → `{"ok":true,"islem_id":"19c0cfd4-…"}`. Not: bu probe demo verisinde bir tohumlamayı Gebe işaretledi (demo yazması bilinçli serbest; sonraki "Prod'dan Klonla" ile sıfırlanır).

## 4. kritik-akis.spec.js — skip açılımı + bulunan latent test bug'ı

2 gerekçeli skip (46fbb4d, O2 gerekçesi) kaldırıldı; O2 çözüldüğü için gerekçeleri geçersizdi. Yalnız bu dosyaya dokunuldu; `STUB` import'u kullanım kalmayınca imzadan çıkarıldı. GitNexus `impact`: spec dosyası/sembolleri indeksli değil (uygulama grafı dışı) → blast radius yok; commit öncesi `detect_changes` koşuldu.

**İlk koşumda B20 fail aldı (65/1/1) — latent bug, bu skip'ler açılana kadar hiç koşmamış bir dal:**
- B20'nin `gebeSayi==0` dalı `ilkBekliyoruGebeYap` ile üreme sayfasında kalıyor; test dashboard'a dönmeden `.sc.ok` "Gebe ›" kartına tıklıyor → kart görünmez → timeout. Stub modunda bu dala girilmiyordu (stub verisinde Bekliyor yok → veri-agnostic skip); demo modunda ise O2 skip'i vardı. Demo RPC'leri düzelince dal ilk kez koştu ve patladı.
- **Fix (test tarafı):** poll sonrası `await navTo(page, '#nb-dash');` eklendi — testin niyeti (dashboard kartı → chip debounce davranışı) korunur.
- Doğrulama: `npx playwright test tests/kritik-akis.spec.js` → **3 passed**.

## 5. Tam koşum (GERÇEK demo modu — IDLE-GÖREV.md'deki komut, birebir)

**Final koşum: 66 passed / 0 failed / 1 skipped (5.3 dk) — hedef 66/0/≤2 TUTTU.** Komut: IDLE-GÖREV.md'deki docker komutu birebir (`PLAYWRIGHT_DEMO_MODE=1`, stub'sız, `--workers=1`, `mcr.microsoft.com/playwright:v1.58.2-noble`).

| Spec | Test | Sonuç |
|---|---|---|
| tests/e2e.spec.js | 43 | 43 pass |
| tests/smoke.spec.js | 9 | 9 pass |
| tests/kritik-akis.spec.js | 3 | **3 pass** (2 skip açıldı — artık gerçek demo DB'siyle koşuyor) |
| tests/modal-router.spec.js | 5 | 5 pass |
| tests/gece-tarih.spec.js | 3 | 2 pass + 1 skip |
| tests/offline-kuyruk.spec.js | 2 | 2 pass |
| tests/sablon.spec.js | 1 | 1 pass |
| tests/sutten-kes.spec.js | 1 | 1 pass |

Kalan 1 skip'in gerekçesi: `gece-tarih.spec.js:63` — "clock 01:00: bugünün doğum tarihi 'ileri tarih' diye reddedilmez". Veri-race dalı: clock'lu sayfada `animals` pull'u tamamlanmamışsa test veri-agnostic `!anneKupe` koluna düşer (e2e-gercek run5'te de buydu). Veriye bağımlı asserted'i zorlamak testi kararsız kılar — bilinçli bırakıldı (IDLE-GÖREV: "1 veri-race skip kalabilir").

Bu koşumdan önce 1 pilot koşum yapıldı (65/1/1): B20'nin hiç koşmamış `gebeSayi==0` dalı ilk kez açıldığında dashboard'a dönüş eksikliği patladı — bölüm 4'te fix'i var.

## 6. O1 ÖNERİSİ — demo'da anon GRANT (uygulama YOK, karar kullanıcıda)

Mevcut durum (ölçülmüş): `information_schema.role_table_grants` → anon: **1** tablo grant'ı (kalıntı), authenticated: 234. Anon REST `GET /rest/v1/hayvanlar` → **401**. 2026-09 restore sonrası "yalnız authenticated" durumunda.

**GRANT restore etme (anon'a SELECT geri ver) — artı:**
- Test altyapısı oturumsuz basit client'la DB doğrulaması yapabilir (e2e-gercek'te `ensureSession` ile çözüldü ama ek karmaşıklık).
- Demo "herkese açık oyuncak" felsefesiyle tutarlı: kimlik girmeden incelemek isteyen anon okuyabilir.
- `hekim_listesi` gibi bazı RPC/'ler anon varsayımıyla yazılmışsa (O3'e benzer eksikler) restore daha az sürpriz verir.

**GRANT restore etmeme (mevcut "yalnız authenticated") — artı:**
- Prod verisinin kopyası olan demo verisi için maruziyet yüzeyi küçülür: UI demo autologin zaten authenticated sağlıyor; anon okuma yalnız ekstra risk taşır, işlev görmez.
- Restore/klon akışı GRANT'ları geri alıyor — restore edilirse bir sonraki restore'da yeniden kaybolur (kalıcılık için demo kurulum script'ine adım eklemek gerekir → bakım yükü).
- Test altyapısı artık oturumlu okuyor; anon GRANT'a ihtiyaç duyan hiçbir mevcut akış yok.

**Öneri:** **"Yalnız authenticated" kalsın.** Faydası olan taraf test/oyuncak erişimi zaten autologin'le kapalı; anon SELECT yalnız veri maruziyetini artırır. Eğer ileride oturumsuz public read-only showcase istenirse, `00_grants.sql`'in idempotent bir versiyonu demo kurulum dokümanına eklenerek bilinçli açılır.

## 7. Teslim

- Değişen dosyalar: `tests/kritik-akis.spec.js` (2 skip + latent navTo fix) · `.claude/idle-reports/2026-09-02-demo-sync.md` (bu rapor)
- DB değişiklikleri: yalnız demo projesi (31 migration + trigger ön-adımı + prod_fdw refresh) — repo'ya migration dosyası EKLENMEDİ, mevcut dosyalar değiştirilmedi
- `test-results/` root-sahiplik temizliği koşuldu
- Push YOK — tek commit: `idle: demo-sync — demo şema senkronu + kritik-akis skip açılımı`

## Açık kalemler (kullanıcı kararı bekler)

1. **Prod `gorev_tamamla` hotfix'inin repoya migration olarak işlenmesi** (ASI_PLANLI muafiyeti) + 3 kozmetik gövde farkının normalize edilmesi. Ayrıca demo'daki 3 legacy fonksiyonun (asi_gorev_planla vb.) bilinçli DROP edilip edilmeyeceği.
2. **20260730000002 versiyon çakışması** (iki dosya aynı version) — CLI pipeline'a geçilirse patlar; version'lardan biri kaydırılmalı (migration içeriği değişmez, dosya adı değişir — ayrı görev).
3. **O1** — yukarıdaki öneri: anon GRANT restore EDİLMESİN (karar kullanıcıda).
4. `hekim_listesi` RPC (O3, demo+prod'ta yok) ve `agent-telemetry/tracker.js` 404'ü (O4) — önceki raporlardan, kapsam dışı.
