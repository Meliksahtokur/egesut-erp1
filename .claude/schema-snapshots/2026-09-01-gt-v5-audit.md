# GT v5 AUDIT — `99999999999999_ground_truth.sql` ↔ Canlı Şema (2026-08-31 Snapshot)

> **Amaç:** GT'nin bir sonraki (v5) regen'inden ÖNCE imza düzeyi sapmaların envanteri.
> GT **regen edilmedi** — bu dosya yalnızca denetim tablosudur (Idle-A kapsamı).
> Yöntem: `.claude/schema-snapshots/2026-08-31-live-schema-imzalar.md` (canlı, 189 imza / 185 benzersiz ad)
> ↔ GT `CREATE FUNCTION` ifadeleri (182 ifade / 174 benzersiz ad), Python ile normalize karşılaştırma
> (DEFAULT değerleri atıldı, `without time zone`/`character varying` aliasları normalize edildi, `int`≡`integer`).
> Supabase'e hiçbir çağrı yapılmadı.

## Özet

| Kategori | Adet | Sonuç |
|---|---|---|
| Ortak adda imza düzeyinde EŞLEŞME | **172** | GT gövde regen'i beklerken imza referansı olarak güvenli |
| Canlıda VAR, GT'de YOK (fn) | **11** | GT v5 regen GEREKLİ (denetimli oturum) |
| Ortak adda imza SAPMASI | **2** | GT v5 regen GEREKLİ |
| Tablo id-tip sapması | **2** | GT v5 regen GEREKLİ |
| GT iç tutarsızlığı (duplikat/eksik CREATE) | **5** | GT v5 regen'de tekilleşmeli |
| Kozmetik alias (int↔integer vb.) | 2 alan | Sapma sayılmaz |
| GT'de olup canlıda OLMAYAN fn | **0** | — |

## Sapma Tablosu (22 satır)

| # | Nesne | GT diyor | Canlı (snapshot) diyor | Tip | Denetimli regen? |
|---|---|---|---|---|---|
| 1 | `_guard_dogum_ileri_tarih()` :: trigger | YOK | VAR (2026-08-31, migration 20260831000003) | fn eksik | **EVET** |
| 2 | `_guard_hayvanlar_cinsiyet_grup()` :: trigger | YOK | VAR (2026-08-31; erkek↔grup backend guard'ı burada) | fn eksik | **EVET** |
| 3 | `_guard_tohumlama_yas_cinsiyet()` :: trigger | YOK | VAR (2026-08-31) | fn eksik | **EVET** |
| 4 | `_tohumlama_gorev_uygunluk(p_hayvan_id, p_tarih)` :: text | YOK | VAR | fn eksik | **EVET** |
| 5 | `agent_plans_prune()` :: void | YOK | VAR | fn eksik | **EVET** |
| 6 | `agent_threads_prune()` :: void | YOK | VAR | fn eksik | **EVET** |
| 7 | `planli_tohumlama_kaydet(p_gorev_id uuid, p_hayvan_id, p_tarih, p_sperma, p_hekim_id, p_irk_bilgisi, p_ek_uygulamalar, p_vwp_override)` :: jsonb | YOK | VAR | fn eksik | **EVET** |
| 8 | `search_code(query_embedding vector, match_count)` :: TABLE | YOK | VAR (pgvector) | fn eksik | **EVET** |
| 9 | `search_memory_notes(query_embedding vector, match_count, filter_category)` :: TABLE | YOK | VAR (pgvector) | fn eksik | **EVET** |
| 10 | `tedavi_sablon_tohumlama_gorev_ekle(p_case_id uuid, p_sablon_id uuid)` :: jsonb | YOK | VAR | fn eksik | **EVET** |
| 11 | `vaka_tohumlama_ekle(p_case_id uuid, p_tarih, p_saat time)` :: jsonb | YOK | VAR | fn eksik | **EVET** |
| 12 | `tohumlama_abort` | GT:6535 yalnız **2-param** `(p_tohumlama_id, p_notlar)` | **3-param ana** `(p_tohumlama_id, p_notlar, p_abort_tarihi)` + eski 2-param overload canlıda duruyor | imza sapması (bilinen, 20260830000034) | **EVET** |
| 13 | `_gorev_dinle` | GT:9415 **3-param** `(p_hayvan_id, p_etken_kod, p_ref)` | **4-param** — `p_tarih date` eklenmiş | imza sapması (**YENİ bulgu**) | **EVET** |
| 14 | `tohumlama.id` | GT:116 `id text` | **uuid** | tablo id-tip | **EVET** |
| 15 | `stok_hareket.id` | GT:39-40 `id text` | **uuid** (docs denetimi §4 "GT text doğru" demişti — snapshot ÇÜRÜTTÜ; AGENTS.md'ye uuid işlendi) | tablo id-tip | **EVET** |
| 16 | `hekim_listesi()` | GRANT var (GT:2609), **CREATE yok**; tanım migration 20260308000009:321'de | **CANLIDA DA YOK** (185 adda değil); app.js:30 çağırıyor → catch + config fallback | GT iç tutarsızlık + canlı eksik | **EVET** (karar gerekli: canlıya geri mi, GRANT mı silinir) |
| 17 | `add_treatment_day` | GT:1666 + GT:11033 **aynı imza 2 kez CREATE** | tek imza | GT duplikat | regen'de tekilleşir |
| 18 | `update_treatment_time` | GT:913 + GT:11389 2 kez | tek | GT duplikat | regen'de tekilleşir |
| 19 | `set_deneme_no` | GT:875 + GT:1882 2 kez (1882 önceki `NOTIFY pgrst` satırıyla birleşik — format kusuru) | tek | GT duplikat + format | regen'de tekilleşir |
| 20 | `drug_product_ekle` | GT:931 (`public.` şemalı) + GT:5724 (**şemasız** `CREATE FUNCTION drug_product_ekle`) | tek | GT duplikat + şema tutarsızlığı | regen'de tekilleşir |
| 21 | `asi_ekle.p_repeat_interval_days`, `add_vaccination.p_next_offset_days` | GT `integer` | snapshot `int` (≈`integer`) | kozmetik alias | HAYIR |
| 22 | DEFAULT değerleri | GT gösterir | snapshot imza düzeyi DEFAULT göstermez (tasarım) | gösterim farkı | HAYIR |

## Doğrulanan overload eşleşmeleri (shorthand'ler el ile açıldı)

- `hayvan_ekle` — GT:2170 (14 param) + GT:7124 (14 + `p_padok_id uuid`) = snapshot 2 imza ✓
- `hayvan_guncelle` — GT:2621 (p_id + 13) · GT:8218 (+ `p_baba_bilgi`, `p_notlar`, `p_anne_id`, `p_padok_id`) ·
  3. CREATE (+ `p_kisir boolean`) = snapshot 3 imza ✓
- `asi_guncelle` (p_vaccine_id + asi_ekle'nin 13 alanı) ✓ · `drug_class_guncelle` (p_id + 4) ✓ ·
  `drug_guncelle` (p_id + 5) ✓
- `tohumlama_kaydet` 7 param + DEFAULT'lar GT ile uyumlu (snapshot DEFAULT göstermez) ✓

## Eşleşen diğer kritik imzalar (örneklem, üçlü kaynak)

`create_case(p_animal_id, p_disease_id uuid, p_notes)` · `dogum_kaydet(8p)` · `cikis_yap(5p, cikis_tarihi DEFAULT now()::date)` ·
`kupe_musait_mi(3p)` · `buzagi_sutten_kesme_onayla(2p)` · `stat_suru_ozet(2p)` · `stat_gebelik_ozet(5p)` ·
`hastalik_kaydet(9p)` · `kizginlik_vaka_ac(4p)` · `tedavi_tekrar... tohumlama_tekrar_kaydet(5p)` — hepsi GT = canlı.

## Sonuç — GT v5 regen gereksinimi

- **Denetimli regen GEREKLİ (17 madde):** #1-16 (11 eksik fn + 2 imza sapması + 2 id-tip + hekim_listesi kararı).
  Gövde metni (`pg_get_functiondef`) ancak denetimli oturumda çekilebilir — idle görev DB'ye dokunmaz.
- **Regen'de otomatik düzelir:** #17-20 (GT iç duplikatları).
- **İşlem gerekmez:** #21-22.
- Not: snapshot dosya başlığı "195 giriş" der; fiziksel imza satırı 189, benzersiz ad 185. Fark muhtemelen
  sayım yöntemi (trigger/event ayırt etmeksizin pg_proc satır sayısı); regen oturumunda netleşmeli.
