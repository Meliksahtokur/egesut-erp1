[DÜŞÜK] tests/unit/vaka-toplu-ac.test.js:2211 — Madde 1 kaldırma grep'i eski sembolü yorumda buluyor — W3 hedefinde `git grep -n 'bcTarihTakvim' agent/tarih-secici-standardi-W3 -- index.html js tests` çıktısı `// 20260913-16: F3 kopya birleşmesi — bcTarihTakvim* kalktı...`; `bcTarihSecimEkle` prod-öksüzü beyanı ise doğru (yalnız js/forms.js:988 ve test referansları).
2 TEMİZ — W13 ay geçişi korunmuş — `git diff` eski `Date` hesabından `tarihAyIzgara` çekirdeğine geçişi gösterdi; 1900-2100 arasındaki 2.412 ay karşılaştırmasında `mismatches: 0`, W3 odak testleri `263/263`.
3 TEMİZ — caseGunModalRender ortak grid'i ve Set davranışı korunmuş — `js/ui.js:6658` `tarihAyIzgara(yil, ay + 1)` kullanıyor, `js/ui.js:6700-6703` toggle Set akışı değişmemiş; W3 W13 modal testleri yeşil.
4 TEMİZ — handlers repoint tek satır ve rota anahtarı sabit — `js/utils/handlers.js:228` yalnız `bcTarihSeciciAc()` çağırıyor, `index.html:1185` `data-action="bc-tarih-takvim"` olarak kalıyor; W21 handler testi geçti.
5 TEMİZ — üç davranış delta'sı veri kaybı açmıyor — değer ayı `forms.js:1820` ile aktarılıyor, aralık dışı `ui.js:6788-6796` ataması yapmadan dönüyor, Onayla `ui.js:6798-6815` doğrulamadan `onSec` çağırmıyor; bağımsız min/max probe `onSec_calls=0, modal_open=true`.
6 TEMİZ — `?v=` bütünlüğü sağlanmış — W3 `index.html` ölçümü `23 v=20260913-16`, W21 damga muhafızı `manifest.json?v=20260913-16` ve eski damgaların yokluğunu doğruladı.
7 TEMİZ — testler gerçek UI fonksiyonlarını ortak document stub'ında sürüyor ve göreli tarih kullanıyor — W3 hedefinde yeni 7 DOM testi `tarih-saf.test.js` içinde `7/7` geçti; tam unit tabanı `824/823/1`, W3 `831/830/1`, aynı tek `_gmGroupHtml` kırmızısı.
8 TEMİZ — yeni/ortak render'da güvenilmeyen ham interpolasyon yok — grid hücreleri `tarihAyIzgara` üretimi olan sayısal `h.iso/h.gun` değerlerinden geliyor (`ui.js:6873-6881`), başlık/hata/input değerleri `esc`/`escAttr` ile kaçırılıyor (`ui.js:6885`, `6902`, `6905`).
[DÜŞÜK] .claude/reviews/2026-09-13-tarih-secici-f3-teslim.md:27 — teslim raporu aktif manifesti eskimiş biçimde bildiriyor — rapor “`handlers.js` goal write_manifest'inde yoktur” diyor, fakat aktif goal `.harness/goals/2026/G-20260913-TARIH-SECICI.md:16` bu yolu “ratified by lead 2026-09-13” olarak listeliyor; F4 guard/karar/ui-map dosyalarında W3 diff'i yok.
SONUÇ: REVİZYON
